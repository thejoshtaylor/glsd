{
  "name": "glsd",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter server-frontend dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
