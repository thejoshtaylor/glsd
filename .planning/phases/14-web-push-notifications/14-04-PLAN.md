---
phase: 14-web-push-notifications
plan: 04
type: execute
wave: 4
depends_on: [14-02, 14-03]
files_modified:
  - server/backend/app/alembic/versions/p14_001_add_push_subscription.py
autonomous: false
requirements:
  - NOTF-01
  - NOTF-02

must_haves:
  truths:
    - "push_subscription table exists in the live database"
    - "All backend tests pass with migration applied"
    - "Frontend builds without errors"
  artifacts:
    - path: "server/backend/app/alembic/versions/p14_001_add_push_subscription.py"
      provides: "Applied migration"
      contains: "push_subscription"
  key_links:
    - from: "alembic upgrade head"
      to: "PostgreSQL"
      via: "Alembic migration runner"
      pattern: "push_subscription"
---

<objective>
Schema push, full test suite, frontend build verification, and human verification of push notification functionality.

Purpose: Ensures the database schema is updated, all tests pass, the frontend compiles cleanly, and a human verifies the end-to-end push notification flow works in a real browser.

Output: Migration applied, tests green, build clean, human-verified push flow
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-web-push-notifications/14-CONTEXT.md
@.planning/phases/14-web-push-notifications/14-01-SUMMARY.md
@.planning/phases/14-web-push-notifications/14-02-SUMMARY.md
@.planning/phases/14-web-push-notifications/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply Alembic migration and run full test suite</name>
  <files>
    server/backend/app/alembic/versions/p14_001_add_push_subscription.py
  </files>
  <read_first>
    server/backend/app/alembic/versions/p14_001_add_push_subscription.py
    server/backend/app/models.py
  </read_first>
  <action>
**1. Apply Alembic migration:**
```bash
cd /Users/josh/code/glsd/server/backend && alembic upgrade head
```
Verify the push_subscription table exists by checking the output or querying the database.

**2. Verify migration applied successfully:**
```bash
cd /Users/josh/code/glsd/server/backend && alembic current
```
Output should show the head revision. If it does not, investigate with `alembic heads` for multiple heads or migration conflicts.

**3. Run full backend test suite:**
```bash
cd /Users/josh/code/glsd/server/backend && python -m pytest tests/ -x -q --timeout=30
```
All tests must pass including the new test_push_service.py, test_push_routes.py, and test_push_dispatch.py.

**4. Run frontend build:**
```bash
cd /Users/josh/code/glsd/server/frontend && npx tsc --noEmit && npm run build
```
TypeScript compilation and Vite build must succeed.

**5. Verify service worker is served correctly:**
```bash
cd /Users/josh/code/glsd/server/frontend && ls -la dist/sw.js 2>/dev/null || ls -la public/sw.js
```
Confirm sw.js exists in the build output (Vite copies public/ files to dist/).

If any test fails, investigate and fix the issue before proceeding. Common issues:
- Migration conflict: run `alembic heads` to check for multiple heads
- Import errors: verify pywebpush is installed
- Test auth: verify test JWT generation works with current SECRET_KEY
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd/server/backend && alembic upgrade head && alembic current 2>&1 | tail -3 && python -m pytest tests/ -x -q --timeout=30 2>&1 | tail -5 && cd /Users/josh/code/glsd/server/frontend && npx tsc --noEmit 2>&1 | tail -5 && echo "All checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` completes without error (migration applied)
    - `alembic current` shows the head revision
    - `python -m pytest tests/ -x -q` exits 0 with all tests passing
    - `npx tsc --noEmit` exits 0 (or only pre-existing non-blocking errors)
    - push_subscription table exists in database
  </acceptance_criteria>
  <done>Database migrated, all tests passing, frontend compiles cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify push notification end-to-end flow</name>
  <files>n/a</files>
  <action>
Human verifies the complete Web Push notification system:
- Push notification subscription management (Settings > Notifications tab)
- Service worker with push event handling, notification display, and Approve/Deny action buttons
- PWA installability with manifest, icons, and install banner
- Backend push dispatch from WebSocket event handlers (permissionRequest, taskComplete)
- REST API for subscription CRUD and service worker permission responses

Verification steps:
1. Start the backend: `cd server/backend && uvicorn app.main:app --reload`
2. Start the frontend: `cd server/frontend && npm run dev`
3. Open http://localhost:5173 in Chrome

**PWA Install Banner (D-06):**
4. Check that a blue install banner appears at the top of the page (may only appear on HTTPS or after installing as PWA)
5. If visible, click "Dismiss" -- verify it does not reappear on refresh

**Settings Push Toggles (D-08, D-09, D-10):**
6. Navigate to Settings > Notifications tab
7. Find the "Push Notifications" card section
8. Toggle "Enable push notifications" ON -- browser should prompt for notification permission
9. If granted, verify the toggle stays ON
10. Verify "Permission requests" and "Session completions" sub-toggles are enabled
11. Toggle "Session completions" OFF -- should update without error

**Service Worker (D-05):**
12. Open DevTools > Application > Service Workers -- verify sw.js is registered
13. Open DevTools > Application > Manifest -- verify manifest loads with correct name and icons

**Push Notification (manual -- requires running node + session):**
14. If a node is connected and a session is active, trigger a permissionRequest or taskComplete event
15. Verify an OS notification appears with the correct title/body
16. (Optional) Verify Approve/Deny action buttons work on permissionRequest notification
  </action>
  <verify>Human confirms push flow works or reports issues</verify>
  <done>Human approves the end-to-end push notification flow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic -> PostgreSQL | Migration modifies database schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-12 | Tampering | Alembic migration | mitigate | Migration is idempotent (create table if not exists pattern); downgrade drops table cleanly |
</threat_model>

<verification>
- Alembic migration applied successfully
- Full backend test suite passes
- Frontend TypeScript compilation passes
- Frontend Vite build succeeds
- Human verifies push notification flow in browser
</verification>

<success_criteria>
Database schema updated with push_subscription table, all automated tests pass, frontend builds cleanly, and human confirms push notifications work end-to-end in the browser.
</success_criteria>

<output>
After completion, create `.planning/phases/14-web-push-notifications/14-04-SUMMARY.md`
</output>
