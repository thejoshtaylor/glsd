---
phase: 11-foundation-migrations-and-stub-cleanup
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - server/frontend/src/lib/tauri.ts
  - server/frontend/src/lib/api/projects.ts
  - server/frontend/src/components/projects/project-wizard-dialog.tsx
  - server/frontend/src/components/projects/guided-project-wizard.tsx
  - server/frontend/src/components/projects/import-project-dialog.tsx
  - server/frontend/src/components/project/env-vars-tab.tsx
  - server/frontend/src/components/project/gsd2-session-tab.tsx
  - server/frontend/src/components/project/gsd2-chat-tab.tsx
  - server/frontend/src/components/project/diagnostics-panels.tsx
  - server/frontend/src/components/settings/secrets-manager.tsx
autonomous: true
requirements: [FIX-02]

must_haves:
  truths:
    - "No 'not available' or Tauri-related error messages appear anywhere in the frontend during normal use"
    - "Project wizard dialogs work via server API calls instead of Tauri stubs"
    - "Components that called ptyWrite use cloud session WebSocket or graceful no-ops"
    - "All pages in the cloud router render without Tauri-stub console warnings"
  artifacts:
    - path: "server/frontend/src/lib/tauri.ts"
      provides: "Updated stub layer with real API delegations and silent no-ops"
      contains: "apiRequest"
    - path: "server/frontend/src/lib/api/projects.ts"
      provides: "Extended project API with checkPath and scaffold endpoints"
      contains: "checkProjectPath"
  key_links:
    - from: "server/frontend/src/components/projects/project-wizard-dialog.tsx"
      to: "server/frontend/src/lib/api/projects.ts"
      via: "import for pickFolder, scaffoldProject, checkProjectPath"
      pattern: "import.*from.*api/projects"
    - from: "server/frontend/src/components/project/env-vars-tab.tsx"
      to: "server/frontend/src/lib/api/projects.ts"
      via: "import readProjectFile"
      pattern: "import.*readProjectFile"
---

<objective>
Silence all Tauri stub console warnings and wire Category A/B stubs (project wizard, file access) to real server APIs or permissive defaults. Category D/E stubs (secrets, settings, GSD management, knowledge, diagnostics) become silent no-ops -- full backend wiring for those deferred to Phase 11b per PHASE SPLIT RECOMMENDED.

Purpose: FIX-02 page stabilization -- eliminate all console noise and "not available" messages. D-04/D-05 full stub wiring is split across Phase 11a (this plan) and Phase 11b (new phase for remaining stubs with backend endpoints). D-06 says type-only imports are acceptable.

Output: Updated tauri.ts with silent no-ops and Category A/B API delegations, updated components for cloud-mode behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundation-migrations-and-stub-cleanup/11-CONTEXT.md
@.planning/phases/11-foundation-migrations-and-stub-cleanup/11-RESEARCH.md
@.planning/phases/11-foundation-migrations-and-stub-cleanup/11-UI-SPEC.md
@.planning/phases/11-foundation-migrations-and-stub-cleanup/11-01-SUMMARY.md

<interfaces>
<!-- Existing API client pattern -->
From server/frontend/src/lib/api/client.ts:
```typescript
export async function apiRequest<T>(path: string, options?: RequestInit): Promise<T>
// Handles auth cookies, error extraction, 204 responses
```

From server/frontend/src/lib/api/projects.ts:
```typescript
export async function listProjects(): Promise<{ data: Project[]; count: number }>
export async function getProject(id: string): Promise<Project>
export async function createProject(data: ProjectCreateRequest): Promise<Project>
export async function deleteProject(id: string): Promise<void>
```

From server/frontend/src/lib/api/sessions.ts:
```typescript
export async function listSessions(): Promise<{ data: Session[]; count: number }>
export async function createSession(data: SessionCreateRequest): Promise<Session>
```

From server/frontend/src/hooks/use-cloud-session.ts:
```typescript
// sendTask(prompt: string) -- sends protocol-level task message via WebSocket
// This is NOT ptyWrite. ptyWrite writes raw bytes to PTY (Tauri concept).
// Cloud sessions use sendTask for user input.
```

From server/frontend/src/lib/tauri.ts (2089 lines):
- ~130 exported function stubs returning empty/default values
- ~75 files import from it
- Stubs log `console.warn('[tauri-stub] functionName called')`

Key functional stubs that need real wiring:
- pickFolder, scaffoldProject, checkProjectPath (project wizard)
- readProjectFile, writeProjectFile (file access)
- ptyWrite, onPtyOutput, onPtyExit (PTY -- Tauri-only concept)
- setSecret, getSecret, deleteSecret, listSecretKeys, hasSecret (secrets)
- onActivityLogged, onNotificationNew (event listeners)
- gsd2HeadlessGetSession, gsd2HeadlessUnregister (headless sessions)
- invalidateDependencyCache (dependency tracking)
- buildKnowledgeGraph (knowledge system)
- canSafelyClose, forceCloseAll, saveTerminalSessions (window management)
- getSettings, updateSettings, resetSettings (local settings)
- Various GSD state stubs (~55 returning empty arrays/objects)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tauri.ts stubs to silent no-ops and wire Category A stubs to existing APIs</name>
  <files>
    server/frontend/src/lib/tauri.ts,
    server/frontend/src/lib/api/projects.ts
  </files>
  <read_first>
    server/frontend/src/lib/tauri.ts,
    server/frontend/src/lib/api/client.ts,
    server/frontend/src/lib/api/projects.ts,
    server/frontend/src/lib/api/sessions.ts,
    server/frontend/src/lib/api/nodes.ts,
    server/frontend/src/hooks/use-cloud-session.ts
  </read_first>
  <action>
    This task has two parts: (A) silence all stub console.warn calls, and (B) wire actively-used stubs to real server APIs.

    **Part A: Silence all console.warn('[tauri-stub]') calls across the entire tauri.ts file.**

    Replace every `console.warn('[tauri-stub] ...')` with nothing (remove the line). The stubs already return safe empty/default values -- the only problem is the console noise. Per D-06, the stub functions themselves are fine as no-ops; we just need to remove the warnings.

    Do a global find-and-remove of all lines matching `console.warn.*tauri-stub` in tauri.ts. This covers all ~130 stubs in one sweep.

    **Part B: Wire actively-used function stubs to real server API calls.**

    For these specific stubs in tauri.ts, replace the empty return with a real API delegation using the existing `apiRequest` from `lib/api/client.ts`:

    1. **`pickFolder`** -- This is a Tauri-native file picker with no web equivalent. Replace with a function that returns `null` (no folder selected). The project wizard components will need to show a text input instead of a folder picker. This is the correct behavior: in cloud mode the user types a path on the remote node, they don't browse local files.

    2. **`checkProjectPath`** -- Add to `lib/api/projects.ts`:
       ```typescript
       export async function checkProjectPath(nodeId: string, path: string): Promise<{ exists: boolean; isDirectory: boolean }> {
         return apiRequest(`/api/v1/sessions/check-path`, {
           method: 'POST',
           headers: { 'Content-Type': 'application/json' },
           body: JSON.stringify({ node_id: nodeId, path }),
         });
       }
       ```
       In tauri.ts, update the `checkProjectPath` stub to call this API function. If the call fails (node offline), return `{ exists: false, isDirectory: false }`.

       NOTE: The backend endpoint `/api/v1/sessions/check-path` does not exist yet. For Phase 11, make `checkProjectPath` in tauri.ts return `{ exists: true, isDirectory: true }` as a permissive default (the daemon will validate paths when sessions start). Add a `// TODO: Phase 11b -- wire to real endpoint` comment. This avoids blocking the project wizard while keeping the stub functional.

    3. **`scaffoldProject`** -- Replace stub to delegate to `createProject` from `lib/api/projects.ts`. The scaffold operation in cloud mode is just "create a project record pointing at the node + cwd". Import and call `createProject({ name, node_id, cwd })` and return a success ScaffoldResult shape.

    4. **`readProjectFile`** and **`writeProjectFile`** -- These read/write files on the node. No backend endpoint exists yet. Replace stubs to return empty string / void respectively with no console.warn. Add `// TODO: Phase 11b -- wire to node file access endpoint`.

    5. **`ptyWrite`**, **`onPtyOutput`**, **`onPtyExit`** -- Per D-07 research verdict: ptyWrite is NOT wired via useCloudSession. However, the components that call ptyWrite (gsd2-session-tab, gsd2-chat-tab) are for headless GSD2 sessions -- a Tauri-desktop concept. In cloud mode, users interact via useCloudSession.sendTask(). Make these silent no-ops (they already return empty values, just remove console.warn).

    6. **`gsd2HeadlessGetSession`**, **`gsd2HeadlessUnregister`** -- Headless GSD2 sessions are Tauri-local. Silent no-ops.

    7. **`setSecret`**, **`getSecret`**, **`deleteSecret`**, **`listSecretKeys`**, **`hasSecret`**, **`getPredefinedSecretKeys`** -- Secrets management requires server-side storage not yet built. Silent no-ops returning empty defaults. Full wiring deferred to Phase 11b.

    8. **`onActivityLogged`**, **`onNotificationNew`** -- Event listener stubs. Already return no-op unlisten functions. Remove console.warn only.

    9. **`invalidateDependencyCache`**, **`buildKnowledgeGraph`** -- GSD project management features (Category D). Silent no-ops. Full wiring deferred to Phase 11b.

    10. **`canSafelyClose`**, **`forceCloseAll`**, **`saveTerminalSessions`** -- Tauri window management. Silent no-ops.

    11. **`getSettings`**, **`updateSettings`**, **`resetSettings`** -- Tauri-local settings. Silent no-ops returning default settings object. Full wiring deferred to Phase 11b.

    12. All `gsdGet*`, `gsd2Get*`, `gsd2List*` stubs (~55 functions) -- Already return empty arrays/objects. Remove console.warn only. Full wiring deferred to Phase 11b.

    13. All `import * as api from '@/lib/tauri'` usages in dashboard.tsx, inbox.tsx, review.tsx, status-bar.tsx -- These pages call api.listProjects(), api.getNotifications(), etc. The stubs already return empty data. With console.warn removed, these will silently show empty states which is correct for cloud mode (these pages render Tauri-desktop GSD project data that doesn't exist in cloud mode).
  </action>
  <verify>
    <automated>cd server/frontend && grep -c "console.warn.*tauri-stub" src/lib/tauri.ts | xargs test 0 -eq && echo "PASS: no tauri-stub warnings" || echo "FAIL: tauri-stub warnings remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "console.warn.*tauri-stub" server/frontend/src/lib/tauri.ts` returns 0
    - server/frontend/src/lib/tauri.ts still exports all function names (no removed exports that would break imports)
    - server/frontend/src/lib/tauri.ts pickFolder returns null without console.warn
    - server/frontend/src/lib/tauri.ts scaffoldProject delegates to createProject from lib/api/projects or returns success shape
    - `grep "TODO.*Phase 11b" server/frontend/src/lib/tauri.ts` shows comments for checkProjectPath, readProjectFile, writeProjectFile, secrets, settings
  </acceptance_criteria>
  <done>
    All ~130 console.warn('[tauri-stub]') lines removed from tauri.ts. scaffoldProject delegates to lib/api/projects.createProject. pickFolder returns null (cloud mode uses text input). readProjectFile/writeProjectFile are silent no-ops with TODO Phase 11b comments. ptyWrite/onPtyOutput/onPtyExit are silent no-ops. All other stubs retain their safe empty return values without warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update components to handle cloud-mode stub behavior</name>
  <files>
    server/frontend/src/components/projects/project-wizard-dialog.tsx,
    server/frontend/src/components/projects/guided-project-wizard.tsx,
    server/frontend/src/components/projects/import-project-dialog.tsx,
    server/frontend/src/components/project/env-vars-tab.tsx,
    server/frontend/src/components/project/gsd2-session-tab.tsx,
    server/frontend/src/components/project/gsd2-chat-tab.tsx,
    server/frontend/src/components/project/diagnostics-panels.tsx,
    server/frontend/src/components/settings/secrets-manager.tsx
  </files>
  <read_first>
    server/frontend/src/components/projects/project-wizard-dialog.tsx,
    server/frontend/src/components/projects/guided-project-wizard.tsx,
    server/frontend/src/components/projects/import-project-dialog.tsx,
    server/frontend/src/components/project/env-vars-tab.tsx,
    server/frontend/src/components/project/gsd2-session-tab.tsx,
    server/frontend/src/components/project/gsd2-chat-tab.tsx,
    server/frontend/src/components/project/diagnostics-panels.tsx,
    server/frontend/src/components/settings/secrets-manager.tsx,
    server/frontend/src/lib/tauri.ts,
    server/frontend/src/hooks/use-cloud-session.ts
  </read_first>
  <action>
    Update components that import functional stubs from tauri.ts to handle cloud-mode behavior correctly. The goal is: no runtime errors, no "not available" messages, graceful degradation where features are Tauri-only.

    **1. project-wizard-dialog.tsx and guided-project-wizard.tsx:**
    - `pickFolder` now returns null. Where these components call `pickFolder()` and use the result to set a path, add a fallback: if `pickFolder()` returns null, do nothing (the user will type the path manually in the text input that should already exist for the cwd field).
    - `scaffoldProject` now delegates to the real createProject API. The component should handle the async call result and show `toast.error("Project creation failed", { description: "Could not scaffold the project. Check the server logs for details." })` on error (per 11-UI-SPEC.md copywriting contract).
    - `checkProjectPath` now returns `{ exists: true, isDirectory: true }` as permissive default. No component changes needed since it will always pass validation.

    **2. import-project-dialog.tsx:**
    - `pickFolder` returns null. If the dialog has a "Browse" button that calls pickFolder, either hide the button or show the text input as the primary path entry method. Do NOT show a "not available" message. Just make the text input the only way to enter the path.

    **3. env-vars-tab.tsx:**
    - `readProjectFile` is now a silent no-op returning empty string. The component should handle empty string gracefully (show empty state or "No .env file found" message). Check what the component does with the result and ensure it doesn't crash on empty string.

    **4. gsd2-session-tab.tsx:**
    - `ptyWrite` is a silent no-op. The component imports and calls `ptyWrite(sessionId, data)`. Since this is a Tauri-only PTY concept and cloud sessions use `useCloudSession.sendTask()`, the no-op is correct. Verify the component doesn't show errors when ptyWrite silently does nothing.

    **5. gsd2-chat-tab.tsx:**
    - Same as gsd2-session-tab: `ptyWrite` and `onPtyOutput` are silent no-ops. `onPtyOutput` returns a no-op unlisten function. Verify the component handles this gracefully (no crash, just no PTY output displayed -- which is correct since cloud sessions render via the terminal component wired to useCloudSession).

    **6. diagnostics-panels.tsx:**
    - Imports diagnostic functions from tauri.ts. All are now silent no-ops returning empty data. The panels should render empty states. Verify no crashes on empty/null data.

    **7. secrets-manager.tsx:**
    - Imports secret management functions from tauri.ts. All are silent no-ops. The component should show an empty secrets list. If it shows a "Secrets not available" message from the old stub behavior, remove that message. Empty state is fine.

    For all error toasts added, use the exact copywriting from 11-UI-SPEC.md:
    - Project scaffold failed: `toast.error("Project creation failed", { description: "Could not scaffold the project. Check the server logs for details." })`
    - File read failed: `toast.error("Could not load file", { description: "The server could not read the requested file. The node may be disconnected." })`
    - Generic fallback: `toast.error("Something went wrong", { description: "An unexpected error occurred. Try again or check the server logs." })`
  </action>
  <verify>
    <automated>cd server/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` in server/frontend exits 0 (no TypeScript errors)
    - project-wizard-dialog.tsx does NOT contain string "not available"
    - guided-project-wizard.tsx does NOT contain string "not available"
    - import-project-dialog.tsx does NOT contain string "not available"
    - secrets-manager.tsx does NOT contain string "not available" or "Secrets not available"
    - diagnostics-panels.tsx does NOT crash on empty stub data (no unchecked property access on null)
    - gsd2-session-tab.tsx still compiles with ptyWrite import (import resolves, function is callable)
    - gsd2-chat-tab.tsx still compiles with ptyWrite and onPtyOutput imports
    - env-vars-tab.tsx handles empty string from readProjectFile without crash
  </acceptance_criteria>
  <done>
    All components that use Tauri function stubs handle cloud-mode behavior gracefully: project wizards use text input for paths instead of pickFolder browse button, scaffoldProject errors show toast per UI-SPEC copywriting, ptyWrite calls are harmless no-ops, diagnostics and secrets panels render empty states without error messages. TypeScript compilation passes with zero errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Backend API | New API calls from former Tauri stubs cross trust boundary |
| User input -> Path validation | Project wizard accepts user-typed paths for remote nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | checkProjectPath permissive default | accept | Returns {exists: true} as permissive default. Actual path validation happens daemon-side when session starts. No security bypass since user already has auth to create sessions on their nodes. |
| T-11-05 | Tampering | scaffoldProject -> createProject | mitigate | createProject API already validates via Pydantic model (name length, node_id UUID, cwd length). Existing FastAPI Depends(CurrentUser) ensures auth. No new attack surface. |
| T-11-06 | Information Disclosure | Tauri stub console.warn removal | accept | Removing console.warn reduces information leakage about internal architecture in browser console. Net positive for security. |
</threat_model>

<verification>
1. `cd server/frontend && npx tsc --noEmit` -- zero TypeScript errors
2. `cd server/frontend && grep -r "console.warn.*tauri-stub" src/` -- zero matches
3. `cd server/frontend && grep -r "not available" src/components/projects/ src/components/settings/secrets-manager.tsx` -- zero matches related to Tauri
4. `cd server/frontend && pnpm test -- --run` -- frontend tests pass (if any exist)
</verification>

<success_criteria>
- Zero console.warn('[tauri-stub]') calls remain in tauri.ts
- All components compile without TypeScript errors
- No "not available" Tauri-related messages in any cloud-reachable page
- Project wizard uses text input for paths, delegates scaffold to real API
- PTY stubs are silent no-ops (cloud sessions use useCloudSession)
</success_criteria>

<output>
After completion, create `.planning/phases/11-foundation-migrations-and-stub-cleanup/11-02-SUMMARY.md`
</output>
