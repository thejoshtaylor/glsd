---
phase: 10-phase-verification-closure
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - AUTH-05
  - AUTH-06
  - SESS-03
  - SESS-04
  - VIBE-01
  - VIBE-02
  - VIBE-03
  - VIBE-04
  - VIBE-05
  - SESS-05
  - RELY-05
  - VIBE-06

must_haves:
  truths:
    - "REQUIREMENTS.md checkboxes for Phase 2 requirements (DAEM-01 through DAEM-04) are all [x]"
    - "REQUIREMENTS.md checkboxes for Phase 3 requirements (AUTH-01 through AUTH-04, SESS-02, SESS-06, RELY-01 through RELY-04) are all [x]"
    - "REQUIREMENTS.md checkboxes for SESS-03, VIBE-02, and VIBE-06 are updated from [ ] to [x]"
    - "REQUIREMENTS.md Traceability table status column matches checkbox state for every updated requirement"
    - "Full pytest suite runs without errors (or documents which tests require live PostgreSQL)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Ground-truth requirements tracking with accurate checkbox state"
      contains: "\\[x\\] \\*\\*SESS-03\\*\\*"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "04-VERIFICATION.md and 05-VERIFICATION.md"
      via: "Checkbox state reflects verification report findings"
      pattern: "Complete"
---

<objective>
Synchronize REQUIREMENTS.md checkboxes with the verification evidence gathered in Plan 01, and run the full pytest suite to confirm test health.

Purpose: REQUIREMENTS.md is the single source of truth for requirement completion. Several requirements were implemented and verified but their checkboxes were never updated. This plan closes that documentation gap and validates backend test suite health.

Output: Updated REQUIREMENTS.md with accurate checkbox state + pytest suite results documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-phase-verification-closure/10-RESEARCH.md

# Verification reports created in Plan 01 (evidence source for checkbox updates)
@.planning/phases/04-frontend-integration/04-VERIFICATION.md
@.planning/phases/05-reliability-and-persistence/05-VERIFICATION.md

# Existing verification reports (evidence for Phase 2/3 checkbox updates)
@.planning/phases/02-daemon-stabilization/02-VERIFICATION.md
@.planning/phases/03-server-relay-and-auth/03-VERIFICATION.md

# Plan 01 summary (confirms verification files were created)
@.planning/phases/10-phase-verification-closure/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md checkboxes and traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state -- MUST read before editing)
    - .planning/phases/02-daemon-stabilization/02-VERIFICATION.md (Phase 2 verification evidence)
    - .planning/phases/03-server-relay-and-auth/03-VERIFICATION.md (Phase 3 verification evidence)
    - .planning/phases/04-frontend-integration/04-VERIFICATION.md (Phase 4 verification evidence -- created in Plan 01)
    - .planning/phases/05-reliability-and-persistence/05-VERIFICATION.md (Phase 5 verification evidence -- created in Plan 01)
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` to update BOTH the checkbox lines AND the Traceability table for each requirement below. Both locations MUST be updated in the same edit to avoid the sync pitfall documented in 10-RESEARCH.md.

**Phase 2 requirements (already verified in 02-VERIFICATION.md, status: passed):**
These are already `[x]` in the checkbox section (DAEM-01 through DAEM-04). Verify the Traceability table rows also say "Complete". If any say "Pending", change to "Complete".

**Phase 3 requirements (verified in 03-VERIFICATION.md, status: human_needed but code-verified):**
Change these from `[ ]` to `[x]` in the checkbox section, and from "Pending" to "Complete" in the Traceability table:
- `AUTH-01` -- User can create an account (verified: `POST /api/v1/users/signup` in users.py)
- `AUTH-02` -- User can log in and stay logged in (verified: `POST /api/v1/login/access-token` returns JWT)
- `AUTH-03` -- User can log out (verified: client-side token discard, no server endpoint needed)
- `AUTH-04` -- Node operator can pair using token (verified: `POST /api/v1/nodes/` create_node_token)
- `SESS-02` -- User can stop a running session (verified: `POST /api/v1/sessions/{id}/stop`)
- `SESS-06` -- already `[x]`, verify traceability says "Complete"
- `RELY-01` -- Server maintains persistent WS to nodes (verified: `/ws/node` endpoint + ConnectionManager)
- `RELY-02` -- Server routes browser WS to correct node/session (verified: ws_browser.py channelId routing)
- `RELY-03` -- Server stores session state in PostgreSQL (verified: SessionModel + SessionEvent tables)
- `RELY-04` -- Node daemon reconnects automatically (verified: welcome message replay handler)

**Phase 4 requirements (verified in 04-VERIFICATION.md created by Plan 01):**
Change these from `[ ]` to `[x]` if not already checked:
- `SESS-03` -- User sees real-time stream output (change `[ ]` to `[x]`)
- `VIBE-02` -- All screens adapted and functional (change `[ ]` to `[x]`)

Update Traceability table:
- `SESS-03` row: change "Pending" to "Complete"
- `VIBE-02` row: change "Pending" to "Complete"

**Phase 5 requirements (verified in 05-VERIFICATION.md created by Plan 01):**
Change this from `[ ]` to `[x]`:
- `VIBE-06` -- Activity feed shows stream of events (change `[ ]` to `[x]`)

Update Traceability table:
- `VIBE-06` row: change "Pending" to "Complete"

**After editing, update the Coverage section at the bottom:**
- Count total `[x]` checkboxes and update the "Pending (gap closure)" line to list only requirements that remain `[ ]`
- The remaining pending requirements should be: INFR-03, INFR-04 (Phase 6 deployment -- not yet executed)

**Validation after edit:** Run `grep -c "\[x\]" .planning/REQUIREMENTS.md` and `grep -c "Complete" .planning/REQUIREMENTS.md` -- the `[x]` count should match the number of "Complete" entries in the Traceability table.
  </action>
  <verify>
    <automated>grep -c "\[x\]" .planning/REQUIREMENTS.md && echo "---" && grep -c "\[ \]" .planning/REQUIREMENTS.md && echo "---" && grep -c "Pending" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains `[x] **AUTH-01**`
    - REQUIREMENTS.md contains `[x] **AUTH-02**`
    - REQUIREMENTS.md contains `[x] **AUTH-03**`
    - REQUIREMENTS.md contains `[x] **AUTH-04**`
    - REQUIREMENTS.md contains `[x] **SESS-02**`
    - REQUIREMENTS.md contains `[x] **SESS-03**`
    - REQUIREMENTS.md contains `[x] **RELY-01**`
    - REQUIREMENTS.md contains `[x] **RELY-02**`
    - REQUIREMENTS.md contains `[x] **RELY-03**`
    - REQUIREMENTS.md contains `[x] **RELY-04**`
    - REQUIREMENTS.md contains `[x] **VIBE-02**`
    - REQUIREMENTS.md contains `[x] **VIBE-06**`
    - Traceability table has no row with "Pending" except INFR-03 and INFR-04
    - `grep -c "\[ \]" .planning/REQUIREMENTS.md` returns exactly 2 (INFR-03 and INFR-04)
  </acceptance_criteria>
  <done>All implemented requirements are checked [x] in both the checkbox section and Traceability table. Only INFR-03 and INFR-04 remain unchecked (Phase 6 not yet executed).</done>
</task>

<task type="auto">
  <name>Task 2: Run full pytest suite and document results</name>
  <files>.planning/phases/10-phase-verification-closure/10-02-SUMMARY.md</files>
  <read_first>
    - server/backend/pyproject.toml (test configuration)
    - .planning/phases/10-phase-verification-closure/10-RESEARCH.md (pytest requirements section)
  </read_first>
  <action>
Run the backend test suite to verify test health. Execute in order:

1. **Foundation tests (no DB required):**
   ```bash
   cd server/backend && python -m pytest tests/test_foundation.py --noconftest -q
   ```
   Record the result (pass count, fail count).

2. **Full test suite (requires PostgreSQL):**
   ```bash
   cd server/backend && python -m pytest tests/ -x -q
   ```
   If this fails due to database connection, record the error and note it in the summary as "requires live PostgreSQL -- human verification needed".

3. **Frontend type check:**
   ```bash
   cd server/frontend && npx tsc --noEmit
   ```
   Record the result.

Document ALL results in the plan summary. If the full pytest suite requires a running PostgreSQL instance that is not available, this is expected and documented -- it does not block the plan.

The summary should include:
- Foundation test results (pass/fail count)
- Full suite results (pass/fail count or "requires live PostgreSQL")
- Frontend TypeScript compilation result
- Overall assessment: which of the 5 Phase 10 success criteria are met
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd/server/backend && python -m pytest tests/test_foundation.py --noconftest -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Foundation tests command was executed and output recorded
    - Full pytest suite command was executed (even if it fails due to no PostgreSQL)
    - Frontend tsc --noEmit command was executed
    - Results are documented in the plan summary
  </acceptance_criteria>
  <done>Test suite results documented. Foundation tests pass. Full suite status recorded (pass or "needs live PostgreSQL"). Frontend compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation only | This plan edits planning documents and runs read-only test commands -- no production code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | REQUIREMENTS.md | accept | Planning document in version control. All changes visible in git diff. Verification files provide evidence trail for each checkbox change. |
| T-10-04 | Repudiation | Checkbox updates without evidence | mitigate | Every checkbox change is traceable to a specific VERIFICATION.md file created in Plan 01. The verification files cite specific source file paths and line numbers. |
</threat_model>

<verification>
- `grep -c "\[x\]" .planning/REQUIREMENTS.md` returns count matching total implemented requirements
- `grep -c "\[ \]" .planning/REQUIREMENTS.md` returns exactly 2 (INFR-03, INFR-04)
- Traceability table "Pending" count matches unchecked count
- Foundation tests pass
</verification>

<success_criteria>
- REQUIREMENTS.md checkboxes accurately reflect verified implementation status
- Both checkbox section and Traceability table are in sync
- Only INFR-03 and INFR-04 remain pending (Phase 6 deployment)
- Test suite results documented
</success_criteria>

<output>
After completion, create `.planning/phases/10-phase-verification-closure/10-02-SUMMARY.md`
</output>
