---
phase: 01-monorepo-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - server/docker-compose.yml
  - server/compose.override.yml
autonomous: true
requirements:
  - INFR-02
must_haves:
  truths:
    - "pnpm install succeeds from the repo root"
    - "pnpm -r build (which runs tsc && vite build in server/frontend) succeeds"
    - "go build ./node/daemon/... succeeds from the repo root"
    - "go test ./node/protocol-go/... succeeds from the repo root"
  artifacts:
    - path: "node_modules/"
      provides: "Installed JS dependencies via pnpm workspace"
    - path: "server/frontend/dist/"
      provides: "Built frontend output from Vite"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "server/frontend/package.json"
      via: "pnpm install resolves workspace member"
      pattern: "server/frontend"
    - from: "go.work"
      to: "node/daemon/go.mod"
      via: "go build resolves local module"
      pattern: "./node/daemon"
---

<objective>
Verify and fix all build pipelines so that pnpm and Go builds pass from the repo root after the restructure.

Purpose: Builds must pass for the phase to be complete. The file moves in Plan 01 may break Docker Compose build context paths. Go and pnpm workspace builds need to be validated.
Output: All builds green -- `pnpm install && pnpm -r build` and `go build ./node/daemon/...` and `go test ./node/protocol-go/...` all exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-foundation/01-RESEARCH.md
@.planning/phases/01-monorepo-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: root config files created -->
<!-- go.work: use ./node/daemon, use ./node/protocol-go -->
<!-- pnpm-workspace.yaml: packages: ["server/frontend"] -->
<!-- package.json: scripts.build = "pnpm -r build" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Docker Compose build context paths and validate Go builds</name>
  <files>server/docker-compose.yml, server/compose.override.yml</files>
  <read_first>
    - server/docker-compose.yml (current state after move -- verify build.context paths)
    - server/compose.override.yml (current state after move -- verify port mappings and service refs)
    - server/backend/Dockerfile (verify COPY source paths relative to build context)
  </read_first>
  <action>
**Step 1: Validate Go builds**

Run from repo root:
```bash
go build ./node/daemon/...
go test ./node/protocol-go/...
```

If these pass, no Go changes are needed. The go.work file from Plan 01 handles local module resolution.

If `go build` fails with module resolution errors, check that go.work.sum was generated. Run `go work sync` if needed.

**Step 2: Fix Docker Compose build context paths**

The `server/docker-compose.yml` was moved from `deployable-saas-template/docker-compose.yml`. Its `build.context` was `.` (relative to the template root). Now that the file lives in `server/`, the context `.` is `server/` which is correct -- `backend/Dockerfile` resolves to `server/backend/Dockerfile`.

Verify the following in `server/docker-compose.yml`:
- `prestart` service: `build.context: .` and `build.dockerfile: backend/Dockerfile` -- these should still work because context=server/ and backend/ is inside server/
- `backend` service: `build.context: .` and `build.dockerfile: backend/Dockerfile` -- same as above
- `frontend` service: `build.context: .` and `build.dockerfile: frontend/Dockerfile` -- this references the OLD template frontend Dockerfile which no longer exists. gsd-vibe does NOT have a Dockerfile.

For the `frontend` service in `server/docker-compose.yml`:
- Comment out the entire `frontend` service block with a note: `# TODO Phase 6: Add frontend Dockerfile and Nginx config for production`
- This is because gsd-vibe has no Dockerfile (it was a Tauri app). Phase 6 creates the production Docker setup.

The `env_file: - .env` references are relative to the docker-compose.yml location (server/), so `server/.env` is the expected location. This is correct since `.env.example` was moved to `server/.env.example`.

**Step 3: Fix compose.override.yml if needed**

Read `server/compose.override.yml`. It likely defines port mappings for development. Verify service names match `server/docker-compose.yml`. If it references a `frontend` service, comment out that section too with the same Phase 6 TODO note.
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd && go build ./node/daemon/... && go test ./node/protocol-go/... && echo "Go builds pass"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./node/daemon/...` exits 0 from repo root
    - `go test ./node/protocol-go/...` exits 0 from repo root
    - `server/docker-compose.yml` does NOT have an active (uncommented) `frontend` service with `build.dockerfile: frontend/Dockerfile`
    - `server/docker-compose.yml` contains a comment with `Phase 6` near the frontend service
    - `server/docker-compose.yml` `backend` service still has `build.context: .` and `build.dockerfile: backend/Dockerfile`
    - `server/docker-compose.yml` `prestart` service still has `build.context: .` and `build.dockerfile: backend/Dockerfile`
  </acceptance_criteria>
  <done>Go workspace builds pass. Docker Compose paths are correct for the new layout. Frontend service commented out pending Phase 6 Dockerfile creation.</done>
</task>

<task type="auto">
  <name>Task 2: Validate pnpm workspace install and frontend build</name>
  <files></files>
  <read_first>
    - pnpm-workspace.yaml (verify content: packages includes server/frontend)
    - package.json (verify workspace scripts)
    - server/frontend/package.json (verify name is "server-frontend", verify build script is "tsc && vite build")
    - server/frontend/tsconfig.json (verify paths and references)
    - server/frontend/vite.config.ts (verify no broken path references)
  </read_first>
  <action>
**Step 1: Run pnpm install from repo root**
```bash
pnpm install
```

This should:
- Read `pnpm-workspace.yaml` and find `server/frontend`
- Install all dependencies listed in `server/frontend/package.json`
- Create `node_modules/` at the repo root (hoisted) and `server/frontend/node_modules/` (package-specific)
- Use the `pnpm-lock.yaml` at repo root (moved from gsd-vibe in Plan 01)

If pnpm install fails:
- Check that `pnpm-lock.yaml` at root is the one from gsd-vibe (not a stale file)
- If lockfile errors occur, delete `pnpm-lock.yaml` and re-run `pnpm install` to regenerate
- The `@tauri-apps/cli` devDependency was removed in Plan 01 Task 2. If other @tauri-apps/* packages fail to install (they shouldn't -- they're pure JS packages), note the error but do NOT remove them yet (Phase 4 handles Tauri removal)

**Step 2: Run pnpm -r build**
```bash
pnpm -r build
```

This runs `tsc && vite build` in `server/frontend/`. 

If `tsc` fails:
- Check for broken import paths. The `@/*` alias maps to `./src/*` in both tsconfig.json and vite.config.ts. Since the entire `src/` directory moved intact, these should still work.
- If there are TypeScript errors from @tauri-apps/* packages (unlikely since they ship .d.ts files), those are pre-existing issues, not caused by the restructure.

If `vite build` fails:
- Check that vite.config.ts is not referencing absolute paths that changed.
- The Tauri env vars (`TAURI_ENV_PLATFORM`, etc.) will be undefined, which is fine -- the config handles this with fallbacks.

**Step 3: Verify the build output exists**
```bash
test -d server/frontend/dist && echo "Frontend build output exists"
```

**Step 4: Run the full validation command from the VALIDATION.md**
```bash
go build ./node/daemon/... && go test ./node/protocol-go/... && pnpm -r build
```

This is the phase gate command. All three must exit 0.
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd && pnpm install && pnpm -r build && echo "pnpm workspace builds pass"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0 from repo root
    - `pnpm -r build` exits 0 from repo root
    - `server/frontend/dist/` directory exists after build
    - `server/frontend/dist/index.html` exists after build
    - Full phase gate passes: `go build ./node/daemon/... && go test ./node/protocol-go/... && pnpm -r build` exits 0
  </acceptance_criteria>
  <done>All build pipelines pass from the repo root. pnpm workspace correctly resolves server/frontend. Go workspace correctly resolves node/daemon and node/protocol-go. Phase 1 success criteria fully met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline | Build commands must not execute untrusted code from moved files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | pnpm install scripts | accept | pnpm install runs package lifecycle scripts. All packages are the same ones already in gsd-vibe's lockfile -- no new dependencies added. Risk is pre-existing and unchanged by the restructure. |
| T-01-04 | Information Disclosure | Docker .env in compose | mitigate | Docker Compose env_file references `server/.env` which is gitignored by root `.gitignore` pattern `.env`. Verified in Plan 01 Task 1. |
</threat_model>

<verification>
Full phase gate (run from repo root):
```bash
# Directory structure
test -d server/backend && test -d server/frontend && test -d node/daemon && test -d node/protocol-go

# No old directories
test ! -d daemon && test ! -d protocol-go && test ! -d deployable-saas-template && test ! -d gsd-vibe

# All builds pass
go build ./node/daemon/...
go test ./node/protocol-go/...
pnpm install && pnpm -r build
```
</verification>

<success_criteria>
- `pnpm install && pnpm -r build` exits 0 from repo root (INFR-02 JS workspace)
- `go build ./node/daemon/...` exits 0 from repo root (INFR-02 Go workspace)
- `go test ./node/protocol-go/...` exits 0 from repo root (INFR-02 Go workspace)
- Docker Compose paths are valid for the new layout
- No feature code was modified -- only structure, config, and import paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-foundation/01-02-SUMMARY.md`
</output>
