---
phase: 04-frontend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/frontend/src/lib/api/client.ts
  - server/frontend/src/lib/api/auth.ts
  - server/frontend/src/lib/api/nodes.ts
  - server/frontend/src/lib/api/sessions.ts
  - server/frontend/src/lib/api/projects.ts
  - server/frontend/src/lib/api/ws.ts
  - server/frontend/src/lib/protocol.ts
  - server/frontend/src/lib/tauri.ts
  - server/frontend/src/lib/queries.ts
  - server/frontend/src/components/shared/project-selector.tsx
  - server/frontend/package.json
  - server/frontend/vite.config.ts
  - server/backend/app/api/routes/ws_browser.py
  - server/backend/app/api/routes/login.py
  - server/backend/app/api/routes/projects.py
  - server/backend/app/api/main.py
  - server/backend/app/models.py
autonomous: true
requirements: [VIBE-01, VIBE-02]

must_haves:
  truths:
    - "No @tauri-apps/* imports remain in production code"
    - "lib/api/ modules export all functions needed by existing query hooks"
    - "lib/tauri.ts still exists but all functions are console.warn stubs"
    - "WebSocket client class connects with cookie auth and routes messages by type"
    - "Protocol TypeScript types match protocol-go/messages.go field names exactly"
    - "Backend login endpoint sets httpOnly cookie"
    - "Backend WS endpoint reads auth from cookie"
    - "Backend Projects resource serves GET/POST/DELETE for user-scoped projects"
    - "React Query hooks for nodes, sessions, and node revocation are available"
    - "ProjectSelector component loads from server Projects API instead of Tauri"
  artifacts:
    - path: "server/frontend/src/lib/api/client.ts"
      provides: "Shared fetch wrapper with cookie auth"
      exports: ["apiRequest"]
    - path: "server/frontend/src/lib/api/ws.ts"
      provides: "WebSocket client with message routing and reconnection"
      exports: ["GsdWebSocket"]
    - path: "server/frontend/src/lib/protocol.ts"
      provides: "TypeScript discriminated union types for all protocol messages"
      exports: ["ProtocolMessage"]
    - path: "server/backend/app/api/routes/projects.py"
      provides: "Projects REST resource (list, create, delete)"
      exports: ["router"]
    - path: "server/backend/app/models.py"
      provides: "Project SQLModel table model"
      contains: "class Project"
  key_links:
    - from: "server/frontend/src/lib/api/client.ts"
      to: "/api/v1/*"
      via: "fetch with credentials: include"
      pattern: "credentials.*include"
    - from: "server/frontend/src/lib/api/ws.ts"
      to: "/ws/browser"
      via: "native WebSocket with cookie auth"
      pattern: "new WebSocket"
    - from: "server/backend/app/api/routes/ws_browser.py"
      to: "websocket.cookies"
      via: "cookie-based JWT extraction"
      pattern: "websocket\\.cookies"
    - from: "server/frontend/src/lib/queries.ts"
      to: "server/frontend/src/lib/api/nodes.ts"
      via: "useNodes, useNode, useRevokeNode hooks"
      pattern: "useNodes|useRevokeNode"
    - from: "server/frontend/src/components/shared/project-selector.tsx"
      to: "server/frontend/src/lib/api/projects.ts"
      via: "listProjects import replacing Tauri"
      pattern: "listProjects|api/projects"
---

<objective>
Create the API client foundation and protocol types that replace Tauri IPC, plus backend cookie auth updates, Projects backend resource, node/session query hooks, and ProjectSelector migration.

Purpose: Establishes the complete REST/WebSocket client layer so all subsequent plans can build UI against real API modules instead of Tauri stubs. Also updates the Phase 3 backend to use httpOnly cookies per D-04, creates the missing Projects backend resource per D-08, adds React Query hooks for nodes/sessions that downstream plans depend on, and updates ProjectSelector per D-10.

Output: `lib/api/` domain-split modules, `lib/protocol.ts` types, gutted `lib/tauri.ts` stubs, updated backend login + WS endpoints, Projects backend resource, query hooks for nodes/sessions in queries.ts, updated ProjectSelector, cleaned Vite config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-integration/04-CONTEXT.md
@.planning/phases/04-frontend-integration/04-RESEARCH.md
@.planning/phases/03-server-relay-and-auth/03-04-SUMMARY.md

<interfaces>
<!-- From protocol-go/messages.go — authoritative field names -->
<!-- Executor MUST read messages.go and replicate all 16 message types exactly -->

From server/backend/app/api/routes/login.py:
- POST /api/v1/login/access-token — returns Token(access_token, token_type)
- Uses OAuth2PasswordRequestForm, crud.authenticate, security.create_access_token

From server/backend/app/api/routes/ws_browser.py:
- @router.websocket("/ws/browser") — currently reads token from query_params.get("token")
- Validates JWT before websocket.accept()
- Routes: task, stop, permissionResponse, questionResponse (browser->server)

From server/backend/app/api/routes/nodes.py:
- GET /api/v1/nodes — list nodes (returns data + count)
- POST /api/v1/nodes/:id/revoke — revoke a node
- Uses CurrentUser dep from app/api/deps.py, SessionDep for DB
- Pattern: router = APIRouter(prefix="/nodes", tags=["nodes"])

From server/backend/app/api/routes/sessions.py:
- POST /api/v1/sessions — create session (nodeId, cwd, prompt)
- GET /api/v1/sessions — list sessions
- POST /api/v1/sessions/:id/stop — stop session

From server/backend/app/models.py:
- User(SQLModel, table=True) with id: uuid.UUID, email, hashed_password
- Node(SQLModel, table=True) with id, user_id, name, machine_id, etc.
- SessionModel(SQLModel, table=True) with id, user_id, node_id, cwd, status
- Pattern: FooBase (shared), Foo (table=True), FooPublic, FoosPublic, FooCreateRequest

From server/backend/app/api/main.py:
- api_router = APIRouter()
- api_router.include_router(nodes.router)
- Pattern: import from app.api.routes, then include_router

From server/frontend/src/lib/tauri.ts:
- Exports ~40+ types and functions (Project, invoke, listen, ptyCreate, ptyWrite, etc.)
- All must become console.warn stubs per D-02

From server/frontend/src/components/shared/project-selector.tsx:
- Uses useProjects() from @/lib/queries
- Uses api.listProjects via tauri stubs
- Props: { selectedProjectId?, basePath, placeholder? }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Projects backend resource (SQLModel + routes + migration)</name>
  <files>
    server/backend/app/models.py,
    server/backend/app/api/routes/projects.py,
    server/backend/app/api/main.py
  </files>
  <read_first>
    server/backend/app/models.py,
    server/backend/app/api/routes/nodes.py,
    server/backend/app/api/deps.py,
    server/backend/app/api/main.py
  </read_first>
  <action>
**Add Project model to models.py (per D-08):**

1. In `server/backend/app/models.py`, add the Project SQLModel following existing patterns (NodeBase/Node/NodePublic/NodesPublic):

```python
# --- Projects (D-08) ---

class ProjectBase(SQLModel):
    name: str = Field(max_length=255)
    node_id: uuid.UUID = Field(foreign_key="node.id")
    cwd: str = Field(max_length=4096)

class Project(ProjectBase, table=True):
    id: uuid.UUID = Field(default_factory=uuid.uuid4, primary_key=True)
    user_id: uuid.UUID = Field(foreign_key="user.id", nullable=False, index=True)
    created_at: datetime | None = Field(
        default_factory=get_datetime_utc,
        sa_type=DateTime(timezone=True),
    )

class ProjectPublic(ProjectBase):
    id: uuid.UUID
    user_id: uuid.UUID
    created_at: datetime | None = None

class ProjectCreateRequest(SQLModel):
    name: str = Field(min_length=1, max_length=255)
    node_id: uuid.UUID
    cwd: str = Field(min_length=1, max_length=4096)

class ProjectsPublic(SQLModel):
    data: list[ProjectPublic]
    count: int
```

**Create projects routes:**

2. Create `server/backend/app/api/routes/projects.py` following the `nodes.py` pattern:
   - `router = APIRouter(prefix="/projects", tags=["projects"])`
   - `GET /` (list) — `session.exec(select(Project).where(Project.user_id == current_user.id))` → return `ProjectsPublic(data=[ProjectPublic.model_validate(p) for p in projects], count=len(projects))`
   - `POST /` (create) — accepts `ProjectCreateRequest` body, creates `Project(user_id=current_user.id, name=body.name, node_id=body.node_id, cwd=body.cwd)`, `session.add(project)`, `session.commit()`, `session.refresh(project)`, returns `ProjectPublic.model_validate(project)`
   - `DELETE /{project_id}` — parse UUID, `session.exec(select(Project).where(Project.id == pid, Project.user_id == current_user.id)).first()`, if not found raise HTTPException(404), `session.delete(project)`, `session.commit()`, return `Message(message="Project deleted")`
   - Use `CurrentUser` and `SessionDep` deps from `app.api.deps`
   - Import `Project, ProjectPublic, ProjectsPublic, ProjectCreateRequest, Message` from `app.models`
   - Import `select` from `sqlmodel`

**Register router:**

3. In `server/backend/app/api/main.py`:
   - Add `from app.api.routes import projects` to imports
   - Add `api_router.include_router(projects.router)` after sessions router

**Generate migration:**

4. Run `cd server/backend && alembic revision --autogenerate -m "add_projects_table"`
  </action>
  <verify>
    <automated>cd server/backend && python -c "from app.models import Project, ProjectPublic, ProjectsPublic, ProjectCreateRequest; print('Models OK')" && grep -q "projects.router" app/api/main.py && echo "Router OK"</automated>
  </verify>
  <acceptance_criteria>
    - server/backend/app/models.py contains `class Project(ProjectBase, table=True)`
    - server/backend/app/models.py contains `class ProjectPublic(ProjectBase)`
    - server/backend/app/models.py contains `class ProjectCreateRequest(SQLModel)`
    - server/backend/app/api/routes/projects.py exists and contains `router = APIRouter`
    - server/backend/app/api/routes/projects.py contains `@router.get("")` or `@router.get("/")`
    - server/backend/app/api/routes/projects.py contains `@router.post("")` or `@router.post("/")`
    - server/backend/app/api/routes/projects.py contains `@router.delete("/{project_id}")`
    - server/backend/app/api/routes/projects.py contains `CurrentUser`
    - server/backend/app/api/main.py contains `projects.router`
    - alembic/versions/ contains a new migration file with `add_projects_table` in its name
  </acceptance_criteria>
  <done>Project SQLModel, ProjectPublic, ProjectCreateRequest models created. Projects REST routes (list, create, delete) with user-scoped ownership. Router registered in main.py. Alembic migration generated.</done>
</task>

<task type="auto">
  <name>Task 2: Backend cookie auth + API client foundation + protocol types + query hooks</name>
  <files>
    server/backend/app/api/routes/login.py,
    server/backend/app/api/routes/ws_browser.py,
    server/frontend/src/lib/api/client.ts,
    server/frontend/src/lib/api/auth.ts,
    server/frontend/src/lib/api/nodes.ts,
    server/frontend/src/lib/api/sessions.ts,
    server/frontend/src/lib/api/projects.ts,
    server/frontend/src/lib/api/ws.ts,
    server/frontend/src/lib/protocol.ts,
    server/frontend/src/lib/queries.ts
  </files>
  <read_first>
    server/backend/app/api/routes/login.py,
    server/backend/app/api/routes/ws_browser.py,
    server/backend/app/core/security.py,
    server/backend/app/core/config.py,
    node/protocol-go/messages.go,
    node/protocol-go/PROTOCOL.md,
    server/frontend/src/lib/tauri.ts,
    server/frontend/src/lib/queries.ts,
    server/frontend/src/lib/query-keys.ts,
    server/backend/app/api/routes/nodes.py,
    server/backend/app/api/routes/sessions.py
  </read_first>
  <action>
**Backend — cookie auth (per D-04):**

1. In `server/backend/app/api/routes/login.py`, add a new endpoint `POST /api/v1/login/cookie` that:
   - Accepts `OAuth2PasswordRequestForm` (same as existing login)
   - Authenticates via `crud.authenticate(session=session, email=form_data.username, password=form_data.password)`
   - On success, creates JWT via `security.create_access_token(user.id, expires_delta=timedelta(minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES))`
   - Returns `JSONResponse(content={"email": user.email, "id": str(user.id)})` with `response.set_cookie(key="access_token", value=token, httponly=True, secure=True, samesite="lax", max_age=int(access_token_expires.total_seconds()), path="/")`
   - Also add `POST /api/v1/logout` that returns `JSONResponse` with `response.delete_cookie(key="access_token", path="/")`
   - Also add `GET /api/v1/me` that reads `access_token` cookie, decodes JWT, returns user info

2. In `server/backend/app/api/routes/ws_browser.py`, update the token extraction to read from cookie first, fallback to query param:
   ```python
   token = websocket.cookies.get("access_token", "")
   if not token:
       token = websocket.query_params.get("token", "")
   ```
   Keep rest of the auth logic identical.

**Frontend — API client foundation (per D-01):**

3. Create `server/frontend/src/lib/api/client.ts`:
   - `const API_BASE = '/api/v1'`
   - Export `async function apiRequest<T>(path: string, options?: RequestInit): Promise<T>` that calls `fetch(API_BASE + path, { ...options, credentials: 'include', headers: { 'Content-Type': 'application/json', ...options?.headers } })`
   - On 401 response: `window.location.href = '/login'` and throw
   - On non-ok: parse `body.detail` or fall back to `HTTP ${response.status}`

4. Create `server/frontend/src/lib/api/auth.ts`:
   - `login(email: string, password: string)` — POST to `/login/cookie` with `URLSearchParams({ username: email, password })` and `Content-Type: application/x-www-form-urlencoded` (OAuth2PasswordRequestForm expects form data)
   - `logout()` — POST to `/logout`
   - `getCurrentUser()` — GET to `/me`

5. Create `server/frontend/src/lib/api/nodes.ts`:
   - `listNodes()` — GET `/nodes` returns `{ data: NodePublic[], count: number }`
   - `getNode(nodeId: string)` — GET `/nodes/${nodeId}`
   - `revokeNode(nodeId: string)` — POST `/nodes/${nodeId}/revoke`
   - `createNodeToken(name: string)` — POST `/nodes/tokens` with `{ name }`
   - Export `NodePublic` interface: `{ id: string, name: string, machine_id: string | null, is_online: boolean, created_at: string, last_seen: string | null, revoked: boolean }`

6. Create `server/frontend/src/lib/api/sessions.ts`:
   - `createSession(nodeId: string, cwd: string, prompt?: string)` — POST `/sessions` with `{ node_id: nodeId, cwd, prompt }`
   - `listSessions(nodeId?: string)` — GET `/sessions` with optional `?node_id=` query param
   - `getSession(sessionId: string)` — GET `/sessions/${sessionId}`
   - `stopSession(sessionId: string)` — POST `/sessions/${sessionId}/stop`

7. Create `server/frontend/src/lib/api/projects.ts`:
   - `listProjects()` — GET `/projects`
   - `getProject(id: string)` — GET `/projects/${id}`
   - `createProject(data: { name: string, node_id: string, cwd: string })` — POST `/projects`
   - `deleteProject(id: string)` — DELETE `/projects/${id}`

8. Create `server/frontend/src/lib/api/ws.ts` — the `GsdWebSocket` class:
   - Constructor takes no args
   - `connect(channelId: string): void` — opens `${location.protocol === 'https:' ? 'wss:' : 'ws:'}//${location.host}/ws/browser?channelId=${channelId}` (no token param — cookie auth per D-04)
   - `on(type: string, handler: (msg: ProtocolMessage) => void): () => void` — registers handler, returns unsubscribe function
   - `send(msg: object): void` — JSON.stringify and send
   - `disconnect(): void` — close WebSocket, clear handlers
   - Private `scheduleReconnect(channelId: string)` — exponential backoff: `Math.min(1000 * 2 ** this.reconnectAttempts, 30000)` delay
   - `onmessage` handler: `JSON.parse(event.data)` then dispatch to matching type handlers
   - `onopen`: reset reconnectAttempts to 0
   - `onclose`: call scheduleReconnect

**Frontend — Protocol types (per PROTOCOL.md):**

9. Create `server/frontend/src/lib/protocol.ts`:
   - Read `node/protocol-go/messages.go` and create matching TypeScript interfaces for ALL 16 message types
   - Browser->Server: `TaskMessage`, `StopMessage`, `PermissionResponseMessage`, `QuestionResponseMessage`, `BrowseDirMessage`, `ReadFileMessage`
   - Server->Browser: `StreamMessage`, `TaskStartedMessage`, `TaskCompleteMessage`, `TaskErrorMessage`, `PermissionRequestMessage`, `QuestionMessage`, `HeartbeatMessage`, `BrowseDirResultMessage`, `ReadFileResultMessage`
   - Field names MUST match Go struct json tags exactly (camelCase)
   - Export discriminated union: `type ProtocolMessage = TaskMessage | StopMessage | ...`
   - Export type guard helpers: `isStreamMessage(msg)`, `isPermissionRequest(msg)`, etc.

**Frontend — Add node/session query hooks to queries.ts (per Blocker 2 fix):**

10. Edit `server/frontend/src/lib/queries.ts`:
   - Add imports at the top: `import * as nodesApi from './api/nodes'` and `import * as sessionsApi from './api/sessions'`
   - Add these hooks (used by Plans 03 and 04):
     ```typescript
     // Nodes
     export function useNodes() {
       return useQuery({ queryKey: ['nodes'], queryFn: nodesApi.listNodes });
     }
     export function useNode(nodeId: string) {
       return useQuery({ queryKey: ['nodes', nodeId], queryFn: () => nodesApi.getNode(nodeId), enabled: !!nodeId });
     }
     // Sessions
     export function useSessions(nodeId?: string) {
       return useQuery({ queryKey: ['sessions', nodeId], queryFn: () => sessionsApi.listSessions(nodeId) });
     }
     // Node revocation mutation
     export function useRevokeNode() {
       const queryClient = useQueryClient();
       return useMutation({ mutationFn: nodesApi.revokeNode, onSuccess: () => { void queryClient.invalidateQueries({ queryKey: ['nodes'] }); } });
     }
     ```
   - These hooks depend only on `lib/api/nodes.ts` and `lib/api/sessions.ts` which are created in this same task
  </action>
  <verify>
    <automated>cd server/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - server/frontend/src/lib/api/client.ts contains `export async function apiRequest`
    - server/frontend/src/lib/api/client.ts contains `credentials: 'include'`
    - server/frontend/src/lib/api/auth.ts contains `export function login(`
    - server/frontend/src/lib/api/auth.ts contains `export function logout(`
    - server/frontend/src/lib/api/auth.ts contains `export function getCurrentUser(`
    - server/frontend/src/lib/api/nodes.ts contains `export function listNodes(`
    - server/frontend/src/lib/api/nodes.ts contains `export interface NodePublic`
    - server/frontend/src/lib/api/sessions.ts contains `export function createSession(`
    - server/frontend/src/lib/api/ws.ts contains `export class GsdWebSocket`
    - server/frontend/src/lib/api/ws.ts contains `scheduleReconnect`
    - server/frontend/src/lib/api/ws.ts does NOT contain `token=` in the WebSocket URL
    - server/frontend/src/lib/protocol.ts contains `type ProtocolMessage =`
    - server/frontend/src/lib/protocol.ts contains `TaskMessage`
    - server/frontend/src/lib/protocol.ts contains `StreamMessage`
    - server/frontend/src/lib/protocol.ts contains `PermissionRequestMessage`
    - server/backend/app/api/routes/login.py contains `set_cookie(`
    - server/backend/app/api/routes/login.py contains `httponly=True`
    - server/backend/app/api/routes/login.py contains `def logout`
    - server/backend/app/api/routes/ws_browser.py contains `websocket.cookies.get("access_token"`
    - server/frontend/src/lib/queries.ts contains `useNodes`
    - server/frontend/src/lib/queries.ts contains `useSessions`
    - server/frontend/src/lib/queries.ts contains `useRevokeNode`
    - server/frontend/src/lib/queries.ts contains `nodesApi`
    - server/frontend/src/lib/queries.ts contains `sessionsApi`
  </acceptance_criteria>
  <done>All 6 lib/api/ modules created with typed exports. Protocol types cover all 16 message types. Backend login sets httpOnly cookie. Backend WS reads cookie for auth. Node/session React Query hooks added to queries.ts. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Gut tauri.ts stubs + remove Tauri packages + update Vite config</name>
  <files>
    server/frontend/src/lib/tauri.ts,
    server/frontend/package.json,
    server/frontend/vite.config.ts
  </files>
  <read_first>
    server/frontend/src/lib/tauri.ts,
    server/frontend/package.json,
    server/frontend/vite.config.ts
  </read_first>
  <action>
**Gut tauri.ts (per D-02):**

1. Read `server/frontend/src/lib/tauri.ts` completely — note every exported type and function
2. Keep ALL type exports unchanged (Project, ProjectWithStats, Gsd2Health, etc.) — downstream files import these types
3. Replace ALL function implementations with stubs that:
   - Log `console.warn('[tauri-stub] functionName called — no server equivalent')`
   - Return appropriate empty values: `null` for single objects, `[]` for arrays, `''` for strings, `false` for booleans, `{ completed: true }` for OnboardingStatus
4. Remove the `import { invoke } from "@tauri-apps/api/core"` and `import { listen } from "@tauri-apps/api/event"` lines at the top
5. Remove any other direct `@tauri-apps/*` imports
6. Keep the file at `src/lib/tauri.ts` — it will be incrementally replaced by `lib/api/` modules in later plans

**Remove Tauri packages from package.json:**

7. Edit `server/frontend/package.json`:
   - Remove `@tauri-apps/api` from devDependencies
   - Remove `@tauri-apps/plugin-dialog` from devDependencies
   - Remove `@tauri-apps/plugin-fs` from devDependencies
   - Remove `@tauri-apps/plugin-shell` from devDependencies
   - Remove `@tauri-apps/cli` from devDependencies (if present)
   - Do NOT run `pnpm install` yet (that happens in verify step)

**Update Vite config (per Research Pitfall 5):**

8. Edit `server/frontend/vite.config.ts`:
   - Remove any `process.env.TAURI_ENV_PLATFORM` conditional build targets
   - Set build target to `es2022`
   - Remove `TAURI_` from `envPrefix` (if present)
   - Change dev server port from 1420 to 5173 (standard Vite)
   - Remove `src-tauri` from watch ignore list (if present)
   - Add proxy config for dev server:
     ```typescript
     server: {
       port: 5173,
       proxy: {
         '/api': 'http://localhost:8000',
         '/ws': {
           target: 'http://localhost:8000',
           ws: true,
         },
       },
     }
     ```

9. Run `cd server/frontend && pnpm install` to update lockfile after package.json changes
  </action>
  <verify>
    <automated>cd server/frontend && pnpm install && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - server/frontend/src/lib/tauri.ts does NOT contain `import { invoke } from "@tauri-apps/api`
    - server/frontend/src/lib/tauri.ts does NOT contain `import { listen } from "@tauri-apps/api`
    - server/frontend/src/lib/tauri.ts contains `console.warn`
    - server/frontend/src/lib/tauri.ts contains `export interface Project`
    - server/frontend/package.json does NOT contain `@tauri-apps/api`
    - server/frontend/package.json does NOT contain `@tauri-apps/plugin-dialog`
    - server/frontend/package.json does NOT contain `@tauri-apps/plugin-fs`
    - server/frontend/package.json does NOT contain `@tauri-apps/plugin-shell`
    - server/frontend/vite.config.ts contains `proxy`
    - server/frontend/vite.config.ts contains `/api`
    - server/frontend/vite.config.ts contains `ws: true`
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>tauri.ts is a stub-only file with no @tauri-apps imports. All Tauri packages removed from package.json. Vite config targets es2022 with API/WS proxy. pnpm build succeeds with zero Tauri dependencies.</done>
</task>

<task type="auto">
  <name>Task 4: Update ProjectSelector to use server Projects API (D-10)</name>
  <files>
    server/frontend/src/components/shared/project-selector.tsx
  </files>
  <read_first>
    server/frontend/src/components/shared/project-selector.tsx,
    server/frontend/src/lib/api/projects.ts,
    server/frontend/src/lib/queries.ts
  </read_first>
  <action>
**Update ProjectSelector component (per D-10):**

The component lives at `server/frontend/src/components/shared/project-selector.tsx`.

1. Read current `project-selector.tsx` to identify all Tauri-rooted data paths:
   - Currently uses `useProjects()` from `@/lib/queries` which calls `api.listProjects` from `@/lib/tauri`
   - The `useProjects` hook in queries.ts imports `* as api from "./tauri"` and calls `api.listProjects`

2. Update the `useProjects` hook in `queries.ts` to use the server API instead of Tauri stubs:
   - Change the existing `useProjects` hook's `queryFn` from `api.listProjects` to import and use `listProjects` from `@/lib/api/projects`
   - Similarly update `useProject` hook's `queryFn` from `api.getProject` to `getProject` from `@/lib/api/projects`
   - Add `import * as projectsApi from './api/projects'` to queries.ts
   - Update: `useProjects` queryFn to `projectsApi.listProjects`
   - Update: `useProject` queryFn to `(id) => projectsApi.getProject(id)`

3. The ProjectSelector component itself should NOT need changes — it already uses `useProjects()` from `@/lib/queries`, and by updating the hook's queryFn, data flows from the server automatically.

4. If `project-selector.tsx` has any direct imports from `@/lib/tauri` or `@tauri-apps/*`:
   - Remove those imports
   - Replace with equivalent from `@/lib/api/projects` if needed
   - Keep component structure, UI, and props interface intact (update, not rewrite)
  </action>
  <verify>
    <automated>cd server/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - server/frontend/src/components/shared/project-selector.tsx does NOT contain `@tauri-apps`
    - server/frontend/src/lib/queries.ts contains `projectsApi` or `from './api/projects'`
    - server/frontend/src/lib/queries.ts useProjects hook no longer calls `api.listProjects` from tauri (calls projectsApi.listProjects instead)
    - pnpm build (in server/frontend) exits 0
  </acceptance_criteria>
  <done>ProjectSelector component loads project data from the server Projects API via updated React Query hooks. No Tauri imports remain in the component or its data path. Component structure and UI unchanged per D-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> FastAPI REST | All REST calls authenticated via httpOnly cookie |
| Browser -> FastAPI WS | WebSocket upgrade authenticated via cookie JWT |
| Frontend -> Backend | CORS boundary; credentials: include required |
| Projects API -> DB | User-scoped queries (user_id filter on all project operations) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | login.py cookie endpoint | mitigate | Set httponly=True, secure=True, samesite=lax on access_token cookie |
| T-04-02 | Tampering | ws_browser.py cookie read | mitigate | JWT signature validation before accept(); cookie fallback to query param only during migration |
| T-04-03 | Info Disclosure | client.ts error handling | mitigate | Do not expose server stack traces to UI; only show body.detail string |
| T-04-04 | Elevation of Privilege | client.ts 401 handling | mitigate | Redirect to /login on 401; do not retry with stale credentials |
| T-04-20 | Elevation of Privilege | projects.py | mitigate | All queries filter by user_id == current_user.id; DELETE verifies ownership before deletion |
</threat_model>

<verification>
1. `cd server/frontend && pnpm build` exits 0
2. `grep -r "@tauri-apps" server/frontend/src/` returns zero matches (excluding node_modules)
3. `grep "credentials.*include" server/frontend/src/lib/api/client.ts` returns a match
4. `grep "websocket.cookies" server/backend/app/api/routes/ws_browser.py` returns a match
5. `grep "set_cookie" server/backend/app/api/routes/login.py` returns a match
6. `grep "class Project" server/backend/app/models.py` returns a match
7. `grep "projects.router" server/backend/app/api/main.py` returns a match
8. `grep "useNodes" server/frontend/src/lib/queries.ts` returns a match
9. `grep "useRevokeNode" server/frontend/src/lib/queries.ts` returns a match
</verification>

<success_criteria>
- Frontend builds with zero Tauri dependencies
- All 6 lib/api/ modules exist with typed exports
- Protocol types cover all 16 message types from PROTOCOL.md
- Backend sets httpOnly cookie on login
- Backend WS endpoint reads cookie for auth (fallback to query param)
- Vite dev server proxies /api and /ws to FastAPI
- Backend Projects resource serves GET/POST/DELETE with user-scoped ownership
- React Query hooks for nodes, sessions, and revocation available in queries.ts
- ProjectSelector loads from server Projects API (D-10 satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-integration/04-01-SUMMARY.md`
</output>
