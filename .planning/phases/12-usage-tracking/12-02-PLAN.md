---
phase: 12-usage-tracking
plan: 12-02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - server/frontend/src/lib/format.ts
  - server/frontend/src/lib/api/usage.ts
  - server/frontend/src/lib/query-keys.ts
  - server/frontend/src/lib/navigation.ts
  - server/frontend/src/pages/usage.tsx
  - server/frontend/src/components/usage/usage-summary-cards.tsx
  - server/frontend/src/components/usage/usage-node-breakdown.tsx
  - server/frontend/src/components/usage/usage-daily-chart.tsx
  - server/frontend/src/components/usage/usage-session-table.tsx
  - server/frontend/src/App.tsx
  - server/frontend/src/components/activity/activity-event-item.tsx
  - server/frontend/src/hooks/use-activity-feed.ts
autonomous: true
requirements: [COST-02]

must_haves:
  truths:
    - "User can navigate to /usage from the sidebar"
    - "Usage page shows total cost, total sessions, and avg cost/session in summary cards"
    - "Usage page shows per-node cost breakdown with progress bars"
    - "Usage page shows daily cost bar chart"
    - "Usage page shows paginated session table with 25 rows per page"
    - "Period selector (7d/30d/90d/All) re-fetches all data"
    - "Activity feed taskComplete items display inline cost breakdown"
    - "formatCost returns '$0.00' for zero and '< $0.01' for sub-cent"
  artifacts:
    - path: "server/frontend/src/lib/format.ts"
      provides: "formatCost, formatDuration, formatTokenCount utilities"
      exports: ["formatCost", "formatDuration", "formatTokenCount"]
    - path: "server/frontend/src/lib/api/usage.ts"
      provides: "Usage API client with TypeScript interfaces"
      exports: ["getUsageList", "getUsageSummary"]
    - path: "server/frontend/src/pages/usage.tsx"
      provides: "Usage dashboard page"
      exports: ["UsagePage"]
    - path: "server/frontend/src/components/usage/usage-summary-cards.tsx"
      provides: "Summary stat cards component"
      exports: ["UsageSummaryCards"]
    - path: "server/frontend/src/components/usage/usage-node-breakdown.tsx"
      provides: "Per-node breakdown component"
      exports: ["UsageNodeBreakdown"]
    - path: "server/frontend/src/components/usage/usage-daily-chart.tsx"
      provides: "Daily cost bar chart component"
      exports: ["UsageDailyChart"]
    - path: "server/frontend/src/components/usage/usage-session-table.tsx"
      provides: "Paginated session table component"
      exports: ["UsageSessionTable"]
  key_links:
    - from: "server/frontend/src/pages/usage.tsx"
      to: "/api/v1/usage/ and /api/v1/usage/summary"
      via: "useQuery hooks with usage API client"
      pattern: "getUsageList|getUsageSummary"
    - from: "server/frontend/src/App.tsx"
      to: "server/frontend/src/pages/usage.tsx"
      via: "lazy-loaded Route at /usage"
      pattern: 'path="/usage"'
    - from: "server/frontend/src/lib/navigation.ts"
      to: "/usage"
      via: "NavItem in Infrastructure section"
      pattern: "Usage.*href.*usage"
    - from: "server/frontend/src/components/activity/activity-event-item.tsx"
      to: "server/frontend/src/lib/format.ts"
      via: "formatCost and formatDuration imports"
      pattern: "formatCost|formatDuration"
---

<objective>
Frontend usage dashboard and activity feed enrichment for Phase 12 Usage Tracking.

Purpose: Create the /usage page with summary cards, node breakdown, daily cost chart, and paginated session table (COST-02). Update the activity feed to display cost breakdowns on taskComplete events (D-08). Add shared formatting utilities used across both features.

Output: Working /usage page accessible from sidebar navigation. Activity feed shows cost data on taskComplete events.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-usage-tracking/12-CONTEXT.md
@.planning/phases/12-usage-tracking/12-RESEARCH.md
@.planning/phases/12-usage-tracking/12-UI-SPEC.md
@.planning/phases/12-usage-tracking/12-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 backend endpoints -->

Backend endpoints created in Plan 01:

GET /api/v1/usage/?period={7d|30d|90d|all}&page={N}
Response:
```json
{
  "data": [
    {
      "id": "uuid",
      "session_id": "uuid",
      "node_name": "my-node",
      "input_tokens": 900,
      "output_tokens": 312,
      "cost_usd": 0.04,
      "duration_ms": 42000,
      "created_at": "2026-04-11T10:30:00+00:00"
    }
  ],
  "total": 42,
  "page": 1,
  "page_size": 25,
  "total_pages": 2
}
```

GET /api/v1/usage/summary?period={7d|30d|90d|all}
Response:
```json
{
  "total_cost_usd": 12.34,
  "total_input_tokens": 50000,
  "total_output_tokens": 20000,
  "total_sessions": 42,
  "by_node": [
    { "node_id": "uuid", "node_name": "my-node", "cost_usd": 10.50, "session_count": 30 }
  ],
  "daily": [
    { "date": "2026-04-01", "cost_usd": 1.23 }
  ]
}
```

From server/frontend/src/lib/api/client.ts:
```typescript
export async function apiRequest<T>(path: string, options?: RequestInit): Promise<T>;
```

From server/frontend/src/lib/query-keys.ts:
```typescript
export const queryKeys = {
  // Add usage keys here
  activity: (projectId?: string, limit?: number) => ["activity", projectId, limit] as const,
  allActivity: () => ["activity"] as const,
  ...
};
```

From server/frontend/src/lib/navigation.ts:
```typescript
export interface NavItem { type: 'link'; name: string; href: string; icon: LucideIcon; }
export const navigation: NavigationItem[] = [
  // Infrastructure section at index 11:
  { type: 'section', label: 'Infrastructure' },
  { type: 'link', name: 'Nodes', href: '/nodes', icon: Server },
  // Usage goes after Nodes, before System section
  { type: 'section', label: 'System' },
  ...
];
```

From server/frontend/src/hooks/use-activity-feed.ts:
```typescript
export interface ActivityEvent {
  event_type: string;
  sessionId?: string;
  session_id?: string;
  nodeId?: string;
  message?: string;
  created_at: string | null;
  payload?: Record<string, unknown>;
  sequence_number?: number;
}
```

From server/frontend/src/components/activity/activity-event-item.tsx:
```typescript
export function ActivityEventItem({ event, onClick }: { event: ActivityEvent; onClick?: (event: ActivityEvent) => void }) {
  const message = event.message ?? event.event_type.replace(/_/g, ' ');
  // Currently renders just message text. Needs enrichment for taskComplete cost display.
}
```

From server/frontend/src/App.tsx:
```typescript
// Lazy loading pattern:
const NodesPage = lazy(() => import("./components/nodes/nodes-page").then(m => ({ default: m.NodesPage })));
// Route pattern:
<Route path="/nodes" element={<NodesPage />} />
```

From server/frontend/src/components/layout/page-header.tsx (existing component):
Used as: <PageHeader title="Usage" icon={BarChart3} actions={...} />

Existing UI primitives available in server/frontend/src/components/ui/:
Card, CardHeader, CardContent, CardFooter, Progress, Select, Skeleton, Button, Badge
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared utilities, API client, query keys, navigation, and activity feed update</name>
  <files>
    server/frontend/src/lib/format.ts,
    server/frontend/src/lib/api/usage.ts,
    server/frontend/src/lib/query-keys.ts,
    server/frontend/src/lib/navigation.ts,
    server/frontend/src/components/activity/activity-event-item.tsx,
    server/frontend/src/hooks/use-activity-feed.ts
  </files>
  <read_first>
    - server/frontend/src/lib/api/client.ts (apiRequest signature)
    - server/frontend/src/lib/api/sessions.ts (API module pattern)
    - server/frontend/src/lib/query-keys.ts (existing key factory)
    - server/frontend/src/lib/navigation.ts (full file -- nav items and sections)
    - server/frontend/src/lib/utils.ts (check if format functions already exist)
    - server/frontend/src/components/activity/activity-event-item.tsx (current rendering)
    - server/frontend/src/hooks/use-activity-feed.ts (ActivityEvent interface)
    - .planning/phases/12-usage-tracking/12-UI-SPEC.md (cost/duration format tables, activity feed integration section)
    - .planning/phases/12-usage-tracking/12-CONTEXT.md (D-08, D-10, D-14, D-15, D-16)
  </read_first>
  <action>
    1. CREATE `server/frontend/src/lib/format.ts` with three exported functions per D-15, D-16, D-10:

       `formatCost(usd: number): string` --
       - `usd === 0` returns `"$0.00"` (D-16)
       - `usd > 0 && usd < 0.01` returns `"< $0.01"` (D-15)
       - Otherwise returns `"$" + usd.toFixed(2)` (D-15)

       `formatDuration(ms: number): string` --
       - `ms < 1000` returns `"< 1s"`
       - `ms < 60000` returns `"${Math.floor(ms / 1000)}s"`
       - Otherwise returns `"${Math.floor(ms / 60000)}m${Math.floor((ms % 60000) / 1000)}s"`

       `formatTokenCount(n: number): string` --
       - `n >= 1000` returns `"${(n / 1000).toFixed(1)}k"`
       - Otherwise returns `String(n)`

    2. CREATE `server/frontend/src/lib/api/usage.ts` with TypeScript interfaces and API functions:
       - `UsageRecord` interface: id, session_id, node_name, input_tokens, output_tokens, cost_usd, duration_ms, created_at
       - `UsageListResponse` interface: data (UsageRecord[]), total, page, page_size, total_pages
       - `NodeUsage` interface: node_id, node_name, cost_usd, session_count
       - `DailyUsage` interface: date (string), cost_usd
       - `UsageSummary` interface: total_cost_usd, total_input_tokens, total_output_tokens, total_sessions, by_node (NodeUsage[]), daily (DailyUsage[])
       - `Period` type: '7d' | '30d' | '90d' | 'all'
       - `getUsageList(period: Period, page: number): Promise<UsageListResponse>` -- calls `apiRequest<UsageListResponse>(\`/usage/?period=${period}&page=${page}\`)`
       - `getUsageSummary(period: Period): Promise<UsageSummary>` -- calls `apiRequest<UsageSummary>(\`/usage/summary?period=${period}\`)`

    3. EDIT `server/frontend/src/lib/query-keys.ts` -- add after the `allActivity` key:
       ```typescript
       // Usage
       usage: (period: string, page: number) => ['usage', period, page] as const,
       usageSummary: (period: string) => ['usage', 'summary', period] as const,
       allUsage: () => ['usage'] as const,
       ```

    4. EDIT `server/frontend/src/lib/navigation.ts` per D-14:
       - Add `BarChart3` to the lucide-react import
       - Insert `{ type: 'link', name: 'Usage', href: '/usage', icon: BarChart3 },` after the Nodes entry (after line 44), before the System section

    5. EDIT `server/frontend/src/components/activity/activity-event-item.tsx` per D-08:
       - Import `formatCost` and `formatDuration` from `@/lib/format`
       - After the existing `message` assignment, add a `detail` variable
       - When `event.event_type === 'taskComplete'`: check for cost fields in the event object (they arrive as top-level fields from the SSE broadcast: `event.input_tokens`, `event.output_tokens`, `event.cost_usd`, `event.duration_ms` -- OR in `event.payload` from REST load)
       - Extract: `const payload = (event as Record<string, unknown>);` then check `payload.input_tokens` or `payload.payload?.input_tokens`
       - Build detail string: `in:${inputTokens} out:${outputTokens} . ${formatCost(costUsd)} . ${formatDuration(durationMs)}`
       - Render detail below the message in `text-xs text-muted-foreground font-mono`
       - For backwards compatibility: if cost fields are missing (old events), just show "Task completed" without detail

    6. EDIT `server/frontend/src/hooks/use-activity-feed.ts`:
       - Extend `ActivityEvent` interface to include optional cost fields: `input_tokens?: number`, `output_tokens?: number`, `cost_usd?: number`, `duration_ms?: number`

    7. Verify the frontend builds: `cd server/frontend && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd/server/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - server/frontend/src/lib/format.ts exists and exports formatCost, formatDuration, formatTokenCount
    - format.ts contains `usd === 0` check returning "$0.00"
    - format.ts contains `usd < 0.01` check returning "< $0.01"
    - server/frontend/src/lib/api/usage.ts exists and exports getUsageList, getUsageSummary
    - usage.ts contains `interface UsageRecord` and `interface UsageSummary`
    - query-keys.ts contains `usage:` and `usageSummary:` and `allUsage:`
    - navigation.ts contains `BarChart3` import and `name: 'Usage'` nav item with `href: '/usage'`
    - activity-event-item.tsx imports formatCost and formatDuration from @/lib/format
    - activity-event-item.tsx contains `taskComplete` check with cost detail rendering
    - use-activity-feed.ts ActivityEvent interface contains `input_tokens?: number`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Shared format utilities created. Usage API client with full TypeScript interfaces created. Query keys added. Navigation updated with Usage link in Infrastructure section. Activity feed renders cost breakdown for taskComplete events. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Usage dashboard page with summary cards, node breakdown, chart, and session table</name>
  <files>
    server/frontend/src/pages/usage.tsx,
    server/frontend/src/components/usage/usage-summary-cards.tsx,
    server/frontend/src/components/usage/usage-node-breakdown.tsx,
    server/frontend/src/components/usage/usage-daily-chart.tsx,
    server/frontend/src/components/usage/usage-session-table.tsx,
    server/frontend/src/App.tsx
  </files>
  <read_first>
    - server/frontend/src/pages/dashboard.tsx (page layout patterns, PageHeader usage, useQuery patterns)
    - server/frontend/src/components/layout/page-header.tsx (PageHeader props)
    - server/frontend/src/components/ui/card.tsx (Card, CardHeader, CardContent, CardFooter exports)
    - server/frontend/src/components/ui/progress.tsx (Progress component, check for size/variant props)
    - server/frontend/src/components/ui/select.tsx (Select component API)
    - server/frontend/src/components/ui/skeleton.tsx (Skeleton component)
    - server/frontend/src/components/ui/button.tsx (Button variants)
    - server/frontend/src/App.tsx (route registration and lazy loading pattern)
    - server/frontend/src/lib/api/usage.ts (created in Task 1 -- interfaces and API functions)
    - server/frontend/src/lib/format.ts (created in Task 1 -- formatCost, formatDuration, formatTokenCount)
    - server/frontend/src/lib/query-keys.ts (usage keys added in Task 1)
    - .planning/phases/12-usage-tracking/12-UI-SPEC.md (full layout spec, component inventory, interaction states, copywriting contract)
    - .planning/phases/12-usage-tracking/12-CONTEXT.md (D-11, D-12, D-13)
  </read_first>
  <action>
    1. CREATE `server/frontend/src/components/usage/usage-summary-cards.tsx` per D-11, UI-SPEC:
       - Props: `totalCost: number, totalSessions: number, isLoading: boolean`
       - Three cards in `grid grid-cols-1 sm:grid-cols-3 gap-4`:
         - Total Cost: `formatCost(totalCost)` in `text-3xl sm:text-4xl font-bold` with `[font-feature-settings:'tnum']`
         - Total Sessions: integer display same style
         - Avg Cost/Session: `formatCost(totalSessions > 0 ? totalCost / totalSessions : 0)` same style
       - Loading state: `Skeleton` components in place of values
       - Use Card/CardHeader/CardContent from ui/card

    2. CREATE `server/frontend/src/components/usage/usage-node-breakdown.tsx` per D-11, UI-SPEC:
       - Props: `nodes: NodeUsage[], totalCost: number, isLoading: boolean`
       - Card with CardHeader "Cost by Node"
       - If nodes empty and not loading: "No node activity in this period" in muted text
       - Each node row: name (left), `formatCost(cost)` + `${sessionCount} sessions` (right), Progress bar below
       - Progress value: `(node.cost_usd / totalCost) * 100` (handle totalCost = 0)
       - Sorted by cost descending (sort in component)
       - Loading: 3 Skeleton rows

    3. CREATE `server/frontend/src/components/usage/usage-daily-chart.tsx` per UI-SPEC:
       - Props: `data: DailyUsage[], isLoading: boolean`
       - If data empty and not loading: "No node activity in this period" centered in h-[240px] sm:h-[300px]
       - Otherwise: recharts `ResponsiveContainer` wrapping `BarChart`:
         - `XAxis` dataKey="date" with `tickFormatter` formatting as "MMM DD" via `new Date(d).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })`
         - `YAxis` with `tickFormatter={(v) => \`$${v}\`}` and width=50
         - `Tooltip` with `formatter={(value: number) => [formatCost(value), 'Cost']}` and dark card-styled contentStyle
         - `Bar` dataKey="cost_usd" fill="hsl(var(--primary))" radius={[4,4,0,0]}
       - Chart height: 240px on mobile, 300px on desktop (use className for responsive)
       - Loading: Skeleton in chart area

    4. CREATE `server/frontend/src/components/usage/usage-session-table.tsx` per D-13, UI-SPEC:
       - Props: `data: UsageRecord[], total: number, page: number, totalPages: number, onPageChange: (page: number) => void, isLoading: boolean`
       - Card with CardHeader "Recent Sessions" and page indicator "Page {n} of {total}"
       - HTML table (not Radix Table) with columns: Date, Node, In Tokens, Out Tokens, Cost, Duration
       - Date: `new Date(created_at).toLocaleDateString(...)` short format
       - Node: node_name
       - In/Out Tokens: formatted with `formatTokenCount`
       - Cost: `formatCost(cost_usd)`
       - Duration: `formatDuration(duration_ms)`
       - Table wrapper: `overflow-x-auto` for mobile
       - CardFooter with Prev/Next buttons (variant="outline" size="sm"), Previous disabled on page 1, Next disabled on last page
       - Empty state: "No usage data yet" with description per UI-SPEC copywriting
       - Loading: Skeleton rows (5 rows with Skeleton cells)

    5. CREATE `server/frontend/src/pages/usage.tsx` per D-11, D-12, UI-SPEC:
       - Named export `UsagePage`
       - State: `period` (Period type, default '30d'), `page` (number, default 1)
       - Two useQuery calls:
         - `useQuery({ queryKey: queryKeys.usageSummary(period), queryFn: () => getUsageSummary(period) })`
         - `useQuery({ queryKey: queryKeys.usage(period, page), queryFn: () => getUsageList(period, page) })`
       - Reset page to 1 when period changes
       - Layout (top to bottom):
         - `PageHeader` with title="Usage", icon=BarChart3, description="Track token usage and costs across your sessions"
         - Period selector in page header action slot: Select component with options "7 days"/"30 days"/"90 days"/"All time" mapping to 7d/30d/90d/all
         - `UsageSummaryCards` with data from summary query
         - `UsageNodeBreakdown` with by_node from summary
         - `UsageDailyChart` with daily from summary
         - `UsageSessionTable` with data from list query, pagination props
       - Error state: "Failed to load usage data. Check your connection and try again." per UI-SPEC
       - All sections in `flex flex-col gap-8` (xl spacing between sections)

    6. EDIT `server/frontend/src/App.tsx`:
       - Add lazy import: `const UsagePage = lazy(() => import("./pages/usage").then(m => ({ default: m.UsagePage })));`
       - Add route inside the Suspense-wrapped Routes: `<Route path="/usage" element={<UsagePage />} />`
       - Place after the /nodes routes, before /sessions

    7. Verify build: `cd server/frontend && npx tsc --noEmit && npx vite build`
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd/server/frontend && npx tsc --noEmit && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - server/frontend/src/pages/usage.tsx exists and exports UsagePage
    - usage.tsx contains `useQuery` calls with `queryKeys.usageSummary` and `queryKeys.usage`
    - usage.tsx contains period state with default "30d"
    - usage.tsx renders UsageSummaryCards, UsageNodeBreakdown, UsageDailyChart, UsageSessionTable
    - usage-summary-cards.tsx contains `formatCost` and renders three Card components
    - usage-node-breakdown.tsx contains Progress component and "Cost by Node" heading
    - usage-daily-chart.tsx contains `BarChart` and `ResponsiveContainer` from recharts
    - usage-daily-chart.tsx contains empty state "No node activity in this period"
    - usage-session-table.tsx contains Prev/Next buttons and "Page" text
    - usage-session-table.tsx columns include Date, Node, In Tokens, Out Tokens, Cost, Duration
    - App.tsx contains `path="/usage"` route and lazy import for UsagePage
    - `npx tsc --noEmit` exits 0
    - `npx vite build` exits 0
  </acceptance_criteria>
  <done>Usage dashboard page renders at /usage with summary cards, node breakdown with progress bars, daily cost bar chart, and paginated session table. Period selector changes re-fetch all data. Route registered and lazy-loaded. Navigation link in sidebar. Frontend builds successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST API -> Browser | Usage data served over authenticated REST; frontend renders server-provided data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Information Disclosure | Frontend rendering of usage data | accept | Frontend only renders data returned by authenticated API endpoints (which filter by user_id server-side). No client-side user_id filtering needed. |
| T-12-07 | Tampering | Period selector input | mitigate | Period is a constrained TypeScript union type ('7d'|'30d'|'90d'|'all'). Only valid values are sent to the backend. Backend also validates with regex pattern. |
| T-12-08 | Denial of Service | Excessive API calls from period/page changes | accept | React Query deduplication and caching prevent duplicate requests. Standard user interaction pattern. No rate limiting needed at this layer. |
</threat_model>

<verification>
1. `cd server/frontend && npx tsc --noEmit` -- TypeScript compiles without errors
2. `cd server/frontend && npx vite build` -- production build succeeds
3. Navigate to /usage in browser -- page loads with correct layout
4. Period selector changes re-fetch data (verify via network tab)
5. Activity feed shows cost breakdown on taskComplete events
6. Sidebar shows "Usage" link in Infrastructure section after Nodes
</verification>

<success_criteria>
- /usage page renders with all four sections: summary cards, node breakdown, daily chart, session table
- Period selector (7d/30d/90d/All) re-fetches both endpoints
- Pagination works with Prev/Next buttons
- Activity feed taskComplete items show cost breakdown inline
- Empty states display correct copywriting from UI-SPEC
- Frontend builds and TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/12-usage-tracking/12-02-SUMMARY.md`
</output>
