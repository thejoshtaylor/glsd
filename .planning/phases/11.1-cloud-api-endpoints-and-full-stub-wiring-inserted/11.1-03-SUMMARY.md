---
phase: 11.1-cloud-api-endpoints-and-full-stub-wiring-inserted
plan: 03
subsystem: project-detail-wiring
tags: [activity, sessions, usage, project-filter, sse, frontend-wiring]
dependency_graph:
  requires: [user-settings-api, session-project-id-fk]
  provides: [project-activity-api, project-sessions-api, project-usage-api, sse-activity-feed]
  affects: [server/backend/app/api/routes/activity.py, server/backend/app/api/routes/sessions.py, server/backend/app/api/routes/usage.py, server/frontend/src/lib/queries.ts]
tech_stack:
  added: []
  patterns: [sse-eventsource, project-scoped-query-params]
key_files:
  created:
    - server/frontend/src/lib/api/activity.ts
    - server/backend/tests/api/routes/test_project_filters.py
  modified:
    - server/backend/app/api/routes/activity.py
    - server/backend/app/api/routes/sessions.py
    - server/backend/app/api/routes/usage.py
    - server/backend/app/crud.py
    - server/frontend/src/lib/api/sessions.ts
    - server/frontend/src/lib/api/usage.ts
    - server/frontend/src/lib/queries.ts
    - server/frontend/src/lib/query-keys.ts
    - server/frontend/src/components/project/activity-feed.tsx
decisions:
  - Used object params shape for listSessions to maintain backward compat with existing callers
  - Kept SSE stream as global user stream with client-side project filtering (server-side project-scoped SSE deferred)
metrics:
  duration: 651s
  completed: 2026-04-14T03:04:33Z
  tasks_completed: 3
  tasks_total: 3
  files_created: 2
  files_modified: 9
---

# Phase 11.1 Plan 03: Project Detail Tabs Wiring -- Activity/Sessions/Usage with SSE Summary

Backend project_id query param filtering on activity, sessions, and usage endpoints; frontend API modules for activity/sessions/usage with project-scoped React Query hooks; activity-feed.tsx rewired from Tauri onActivityLogged to SSE EventSource per D-08.

## Task Results

| Task | Name | Commit | Status |
|------|------|--------|--------|
| 1 | Backend -- Add project_id filter to activity, sessions, and usage endpoints | d0a1273 | Done |
| 2 | Frontend -- API modules for activity/sessions/usage + wire project detail tabs with SSE | 80f4559 | Done |
| 3 | Backend tests -- project_id filter behavioral tests | 160ef12 | Done |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] listSessions signature change required caller update**
- **Found during:** Task 2
- **Issue:** Changing `listSessions(nodeId?: string)` to `listSessions(params?: { nodeId?, projectId? })` broke existing caller in queries.ts at line 1120.
- **Fix:** Updated caller from `sessionsApi.listSessions(nodeId)` to `sessionsApi.listSessions(nodeId ? { nodeId } : undefined)`.
- **Files modified:** server/frontend/src/lib/queries.ts
- **Commit:** 80f4559

**2. [Rule 2 - Missing functionality] sessions.ts and usage.ts already existed**
- **Found during:** Task 2
- **Issue:** Plan specified creating sessions.ts and usage.ts but both already existed from prior phases. Updated existing files instead of creating new ones.
- **Fix:** Added project_id support to existing functions rather than overwriting files.
- **Files modified:** server/frontend/src/lib/api/sessions.ts, server/frontend/src/lib/api/usage.ts
- **Commit:** 80f4559

## Verification

- Backend Python files parse without syntax errors (all 4 verified via ast.parse)
- Frontend TypeScript: no errors in any modified/created files (pnpm exec tsc --noEmit shows 0 errors from our files; pre-existing errors in unrelated files like deploy-node-modal, inbox, dashboard)
- activity-feed.tsx contains `new EventSource('/api/v1/activity/stream'` with `withCredentials: true`
- activity-feed.tsx does NOT import `onActivityLogged` or `ActivityEntry` from tauri
- queries.ts useActivityLog calls `activityApi.getActivity` (not `api.getActivityLog`)
- query-keys.ts contains projectSessions, projectUsage, projectUsageSummary
- Test file has 5 test functions verified via AST parse
- Tests require running PostgreSQL (Docker Compose) -- syntax and structure verified

## Threat Mitigations Applied

- T-11.1-05: activity.py filters sessions by `current_user.id` first, then by `project_id` -- user cannot see other users' activity
- T-11.1-06: crud.get_sessions_by_user filters by `user_id` first -- project_id is additional filter within user's own sessions
- T-11.1-07: usage.py joins through SessionModel filtered by `user_id` -- project_id narrows within user's own data
- T-11.1-08: Invalid UUID project_id returns empty results (not 500) -- no information leakage
- T-11.1-09: SSE EventSource uses `withCredentials: true` for cookie auth; stream only delivers authenticated user's events

## Known Stubs

None. All project detail data sources (Activity, Sessions, Usage) flow through real API endpoints. SSE wired for live activity updates.

## Self-Check: PASSED

All 11 files found. All 3 commits (d0a1273, 80f4559, 160ef12) verified in git log.
