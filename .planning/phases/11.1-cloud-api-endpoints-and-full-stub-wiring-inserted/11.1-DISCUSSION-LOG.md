# Phase 11.1: Cloud API Endpoints and Full Stub Wiring — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-13
**Phase:** 11.1-cloud-api-endpoints-and-full-stub-wiring-inserted
**Areas discussed:** Projects page, Settings persistence, Feature gating, Project detail tabs

---

## Projects Page

| Option | Description | Selected |
|--------|-------------|----------|
| Wire to /projects — slim cards | Show project cards with just name, node, working dir. Wire useProjectsWithStats() to the real /projects API. Cards won't have git/cost/roadmap data but the page actually works. | ✓ |
| Redirect to Nodes page | In cloud, 'a project' is really a session on a node. Replace the Projects/Dashboard page with a redirect or stub that points users to the Nodes view instead. | |
| Skip for now | Leave the dashboard stub as-is for this phase. Projects page returns empty. Focus on other stubs first. | |

**Stats gap question:**

| Option | Description | Selected |
|--------|-------------|----------|
| Return slim data, hide missing fields | Wire directly to /projects. Adapt ProjectCard to show name/node/cwd. Hide fields like roadmap_progress, total_cost that don't exist in cloud. | ✓ |
| Add stats to backend endpoint | Extend GET /projects to join usage records and compute total_cost. Return richer response. | |
| Show all fields, display '—' for missing | Keep full ProjectCard UI. Stub fields render as empty/—. | |

**User's choice:** Wire slim — adapt ProjectCard to show available cloud fields, omit missing ones.

---

## Settings Persistence

| Option | Description | Selected |
|--------|-------------|----------|
| localStorage only | Settings stay in the browser. No backend endpoint needed. | |
| Backend endpoint — per-user | Add GET/PUT /users/me/settings. Settings survive across devices. Requires new DB. | ✓ |
| Skip for this phase | Settings page currently broken (stub). Leave it broken. | |

**Storage question:**

| Option | Description | Selected |
|--------|-------------|----------|
| JSON blob column on user table | Add a `settings` JSONB column to the user table. Simple Alembic migration. | |
| Separate user_settings table | New table with typed columns. Schema-enforced. | ✓ |
| Use existing user fields | Repurpose unused fields. | |

**User's choice:** Separate `user_settings` table with typed columns, new migration, new GET/PUT endpoints.

---

## Feature Gating

| Option | Description | Selected |
|--------|-------------|----------|
| Hide from UI | Remove local-only features from nav/pages. Clean, no empty states. | |
| Disable with tooltip | Gray out with "Not available in cloud" tooltip. | |
| Leave as empty states | Current behavior — stubs return [], null, or false. No code changes for local-only stubs. | ✓ |

**User's choice:** Leave as-is. Don't touch local-only stubs. Wire only what has a real cloud equivalent.

---

## Project Detail Tabs

| Option | Description | Selected |
|--------|-------------|----------|
| Show sessions + activity only | Narrow project detail to Sessions + Activity tabs. All other tabs hidden. | |
| Keep all tabs, wire what exists | Keep all 20+ tabs. Wire the ones with backend equivalents, leave rest showing empty state. | ✓ |
| Single summary view | Replace tabs with a simple project summary card. No tabs. | |

**Tabs in scope:**

| Tab | Selected |
|-----|----------|
| Activity feed (wire to GET /activity?project_id=X) | ✓ |
| Sessions tab (wire to GET /sessions?project_id=X) | ✓ |
| Usage / Cost tab (wire to GET /usage?project_id=X) | ✓ |
| Settings page (wire to new GET/PUT /users/me/settings) | ✓ |

**User's choice:** Wire all 4 backend-connected areas. All other tabs stay as empty states from stubs.

---
