---
phase: 5
slug: gsd2-milestones-slices-tasks-ui
status: draft
shadcn_initialized: true
preset: manual (components installed without components.json)
created: 2026-03-21
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui (manual install — no components.json, components present in src/components/ui/) |
| Preset | Not applicable (manual) |
| Component library | Radix UI (via shadcn/ui primitives) |
| Icon library | Lucide React |
| Font | Inter var (sans), JetBrains Mono Variable (mono) |

Source: `tailwind.config.js`, `src/components/ui/` glob, `src/styles/globals.css`

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps (`gap-1`), inline padding, icon-to-label spacing |
| sm | 8px | Compact element spacing (`gap-2`, `p-2`), row internal padding, accordion expand/collapse rows (`py-2`) |
| md | 16px | Default element spacing (`p-4`, `gap-4`), card content padding |
| lg | 24px | Section padding (`p-6`), card header padding |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Empty state vertical padding (`py-12`) |
| 3xl | 64px | Page-level spacing (not used in tab content) |

Exceptions:
- Skeleton rows use `h-4` (16px) height, `h-8` (32px) for wider diff-style skeleton rows

Source: Derived from `gsd2-worktrees-tab.tsx` and `gsd2-health-tab.tsx` patterns

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 (regular) | 1.5 |
| Label / badge text | 12px (`text-xs`) | 400 (regular) | 1.4 |
| Card heading | 14px (`text-sm`) | 600 (semibold) | 1.2 |
| Section subheading | 12px (`text-xs`) | 600 (semibold) | 1.2 |

Font: Inter var. Monospace (task IDs, slice IDs, milestone IDs): JetBrains Mono Variable at 12px (`text-xs font-mono`).

Label/badge text is distinguished from body text by size contrast (12px vs 14px), not by weight. Both body and label/badge text use weight 400.

Source: Derived from `gsd2-health-tab.tsx` `text-sm font-semibold`, established project pattern.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `hsl(var(--background))` | Page background, tab panel background |
| Secondary (30%) | `hsl(var(--card))` / `hsl(var(--muted))` | Cards wrapping each tab, milestone section headers, collapsed accordion rows |
| Accent (10%) | `hsl(var(--primary))` = brand-blue (217 91% 60%) | Active milestone row left border, expand/collapse chevron on hover |
| Destructive | `hsl(var(--destructive))` | Not used in this phase (no destructive actions) |

Accent reserved for:
- Left border stripe on the currently-active milestone row in the Milestones accordion
- Chevron icon color on hover state (`text-primary`)

Status color usage (semantic, not accent):
- Active task icon: `text-yellow-500 animate-pulse` — matches Phase 4 Visualizer exactly (`▶`)
- Pending task icon: `text-muted-foreground` — gray circle (`○`)
- Done milestone/slice badge: `bg-status-success/10 text-status-success border-status-success/30`
- Pending milestone/slice badge: `bg-status-pending/10 text-status-pending border-status-pending/30`
- Error state text: `text-status-error`

Source: `src/styles/globals.css` CSS variables, `src/lib/design-tokens.ts` statusColors, `gsd2-visualizer-tab.tsx` node icon pattern (lines 19–24)

---

## Component Inventory

Components used in this phase (all already present in `src/components/ui/`):

| Component | File | Usage |
|-----------|------|-------|
| Card, CardContent, CardHeader, CardTitle | `card.tsx` | Outer wrapper for each tab, milestone section headers |
| Badge | `badge.tsx` | Done/pending/active status badges on milestone, slice, task rows |
| Skeleton | `skeleton.tsx` | Loading state — placeholder rows while data fetches |
| ScrollArea | `scroll-area.tsx` | Tall task lists within expanded slices |
| Button | `button.tsx` | Not used for primary actions — chevron toggle is plain div/button |
| ChevronRight (Lucide) | n/a | Accordion expand/collapse indicator, rotated 90deg when open |

No new shadcn components need to be installed. No third-party registries.

---

## Interaction Contract

### Accordion — Milestones Tab

Pattern source: `gsd2-worktrees-tab.tsx` `expandedRows: Set<string>` — replicate exactly.

- State: `expandedRows: Set<string>` — one Set for milestone IDs, one for slice IDs within expanded milestones
- Clicking a milestone row toggles its ID in the milestone `expandedRows` Set
- Clicking a slice row (inside an expanded milestone) toggles its ID in the slice `expandedRows` Set
- Expanded state is ephemeral (not persisted — resets on tab re-mount)
- ChevronRight icon: `transition-transform duration-150` — `rotate-90` class applied when expanded
- Accordion rows use `py-2` (8px) for compact scan density
- Expanded slice content: `transition-all duration-150 overflow-hidden` — animate height from 0 to auto via max-height pattern or conditional render

Task loading strategy: **lazy** — slice tasks fetched via `useGsd2Slice` only when that slice row is first expanded (enabled: `expandedRows.has(sliceId)`). This avoids N+1 fetches on initial milestone expand.

### Accordion — Slices Tab

- Milestone section headers are collapsible (ChevronRight toggle, same pattern)
- Slice rows within a milestone section expand inline to show tasks (same lazy load strategy)
- All milestone sections start collapsed on mount
- Accordion rows use `py-2` (8px) for compact scan density

### Tasks Tab

- No accordion — flat list grouped by slice name
- Slice name renders as a `text-xs font-semibold text-muted-foreground uppercase tracking-wide` section divider
- Active task rows render before pending rows within each slice group (sort order: active first, then pending)

---

## Skeleton Contract

Loading state — skeleton rows shown while React Query `isLoading` is true.

| Tab | Skeleton pattern |
|-----|-----------------|
| Milestones | 3 rows: `Skeleton className="h-10 w-full mb-2"` |
| Slices | 2 milestone headers (`h-8 w-1/3`) + 2 slice rows each (`h-8 w-full mb-1`) |
| Tasks | 1 slice divider (`h-4 w-1/4 mb-2`) + 3 task rows (`h-8 w-full mb-1`) |

Pattern matches `gsd2-health-tab.tsx` skeleton approach (inside a Card, no outside spinner).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | No CTA — this phase is read-only display; no user-initiated actions |
| Milestones empty state | "No milestones yet — run a GSD-2 session to get started" |
| Slices empty state | "No slices yet — run a GSD-2 session to get started" |
| Tasks empty state | "No active or pending tasks — all done, or no GSD-2 session has run yet" |
| Milestones error state | "Failed to load milestones — check that the project path is accessible." |
| Slices error state | "Failed to load slices — check that the project path is accessible." |
| Tasks error state | "Failed to load tasks — check that the project path is accessible." |
| Slice load error (inside expanded milestone) | "Failed to load slices for this milestone." |
| Task load error (inside expanded slice) | "Failed to load tasks for this slice." |
| Destructive confirmation | None — no destructive actions in this phase |

Source: Empty state copy locked in CONTEXT.md `## Decisions > Empty states`. Error copy follows `gsd2-health-tab.tsx` pattern: problem description + "check that the project path is accessible."

---

## Status Indicators

Reuse Phase 4 Visualizer icon contract exactly:

| Status | Icon | Class |
|--------|------|-------|
| done | `✔` text character | `text-status-success` |
| active | `▶` text character | `text-yellow-500 animate-pulse` |
| pending | `○` text character | `text-muted-foreground` |

Source: `gsd2-visualizer-tab.tsx` lines 19–24 — copy verbatim into a shared `NodeStatusIcon` helper or inline per component.

Badge variants alongside icons:

| Status | Badge classes |
|--------|---------------|
| done | `bg-status-success/10 text-status-success border-status-success/30` |
| active | `bg-status-warning/10 text-status-warning border-status-warning/30` |
| pending | `bg-status-pending/10 text-status-pending border-status-pending/30` |

Source: `src/lib/design-tokens.ts` statusColors map.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | badge, button, card, skeleton, scroll-area, alert-dialog (existing) | Not required — already installed |
| Third-party | None | Not applicable |

No new components need to be installed or vetted. All required components are already present in `src/components/ui/`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
