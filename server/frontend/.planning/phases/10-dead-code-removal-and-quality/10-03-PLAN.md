---
phase: 10-dead-code-removal-and-quality
plan: 03
type: execute
wave: 1
depends_on: ["10-01", "10-02"]
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/src/commands/pty.rs
  - src-tauri/src/commands/filesystem.rs
  - src-tauri/src/commands/activity.rs
  - src-tauri/src/commands/settings.rs
  - src-tauri/src/commands/dependencies.rs
  - src-tauri/src/commands/logs.rs
  - src-tauri/src/models/mod.rs
autonomous: true
requirements: [DEAD-01, DEAD-02, DEAD-03, DEAD-04, QLTY-01, QLTY-02]
gap_closure: true

must_haves:
  truths:
    - "No unused Rust commands remain registered in lib.rs without an active frontend caller"
    - "pnpm test reports zero failures (130/130 pass)"
    - "pnpm build completes with zero TypeScript errors"
    - "cargo check compiles without errors (icon warning is pre-existing and acceptable)"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "generate_handler![] with only actively-called commands"
      contains: "commands::pty::pty_create"
    - path: "src-tauri/src/commands/pty.rs"
      provides: "PTY commands without dead pty_list_sessions, pty_active_count, pty_close_all"
    - path: "src-tauri/src/commands/dependencies.rs"
      provides: "Dependency commands without dead run_dependency_audit, get_outdated_packages and their helpers"
    - path: "src-tauri/src/commands/activity.rs"
      provides: "Activity commands without dead get_decisions, get_all_decisions, get_decision_categories"
    - path: "src-tauri/src/commands/settings.rs"
      provides: "Settings commands without dead check_claude_status"
    - path: "src-tauri/src/commands/filesystem.rs"
      provides: "Filesystem commands without dead detect_docs_available"
    - path: "src-tauri/src/commands/logs.rs"
      provides: "Log commands without dead rotate_app_logs"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/commands/*.rs"
      via: "generate_handler![] macro entries"
      pattern: "commands::(pty|filesystem|activity|settings|dependencies|logs)::"
---

<objective>
Close the single verification gap from 10-VERIFICATION.md: remove 11 orphaned Rust commands that are registered in lib.rs generate_handler![] but have zero frontend callers in src/.

Purpose: VERIFICATION.md scored 3/4 because success criterion #3 ("No unused Rust commands remain registered in lib.rs without an active frontend caller") failed. Plan 10-01 discovered these 11 commands during audit and deferred them. Plan 10-02 was frontend-only and never addressed them. This gap closure plan removes all 11.

Output: Clean lib.rs with only actively-called commands, dead function bodies deleted, cargo check passes.
</objective>

<execution_context>
@/Users/jeremymcspadden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremymcspadden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dead-code-removal-and-quality/10-CONTEXT.md
@.planning/phases/10-dead-code-removal-and-quality/10-VERIFICATION.md
@.planning/phases/10-dead-code-removal-and-quality/10-01-SUMMARY.md

<interfaces>
<!-- The 11 orphaned commands to remove. Each is registered in lib.rs generate_handler![] but has ZERO callers in src/ -->

From src-tauri/src/lib.rs generate_handler![]:
- Line 137: commands::filesystem::detect_docs_available
- Line 152: commands::activity::get_decisions
- Line 153: commands::activity::get_all_decisions
- Line 154: commands::activity::get_decision_categories
- Line 158: commands::settings::check_claude_status
- Line 171: commands::pty::pty_list_sessions
- Line 174: commands::pty::pty_active_count
- Line 175: commands::pty::pty_close_all
- Line 198: commands::logs::rotate_app_logs
- Line 249: commands::dependencies::run_dependency_audit
- Line 250: commands::dependencies::get_outdated_packages

Commands that MUST STAY (have frontend callers):
- pty: pty_create, pty_write, pty_resize, pty_close, pty_detach, pty_is_active, pty_get_session_info, pty_attach, pty_check_tmux, pty_list_tmux
- filesystem: detect_tech_stack, get_scanner_summary, read_project_file, read_project_docs, pick_folder, list_knowledge_files, list_code_files, search_knowledge_files, write_project_file, delete_project_file, index_project_markdown, build_knowledge_graph
- activity: get_activity_log, search_activity
- settings: get_settings, update_settings, reset_settings, import_settings
- dependencies: get_dependency_status, invalidate_dependency_cache
- logs: get_app_logs, get_app_log_stats, get_log_levels, clear_app_logs, log_frontend_error, log_frontend_event
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 11 orphaned Rust commands from lib.rs and delete function bodies</name>
  <files>
    src-tauri/src/lib.rs,
    src-tauri/src/commands/pty.rs,
    src-tauri/src/commands/filesystem.rs,
    src-tauri/src/commands/activity.rs,
    src-tauri/src/commands/settings.rs,
    src-tauri/src/commands/dependencies.rs,
    src-tauri/src/commands/logs.rs,
    src-tauri/src/models/mod.rs
  </files>
  <read_first>
    src-tauri/src/lib.rs,
    src-tauri/src/commands/pty.rs,
    src-tauri/src/commands/filesystem.rs,
    src-tauri/src/commands/activity.rs,
    src-tauri/src/commands/settings.rs,
    src-tauri/src/commands/dependencies.rs,
    src-tauri/src/commands/logs.rs,
    src-tauri/src/models/mod.rs
  </read_first>
  <action>
    Remove 11 orphaned Tauri commands. For each: delete the line from generate_handler![] in lib.rs AND delete the function body (including #[tauri::command] attribute and doc comment) from the source file. Also delete associated types/structs and helper functions that become dead after the command removal.

    **lib.rs — Remove these 11 lines from generate_handler![]:**
    - `commands::filesystem::detect_docs_available,`
    - `commands::activity::get_decisions,`
    - `commands::activity::get_all_decisions,`
    - `commands::activity::get_decision_categories,`
    - `commands::settings::check_claude_status,`
    - `commands::pty::pty_list_sessions,`
    - `commands::pty::pty_active_count,`
    - `commands::pty::pty_close_all,`
    - `commands::logs::rotate_app_logs,`
    - `commands::dependencies::run_dependency_audit,`
    - `commands::dependencies::get_outdated_packages,`

    **pty.rs — Delete these 3 functions (lines ~133-177):**
    - `pty_list_sessions` (lines 133-140): doc comment + #[tauri::command] + fn body
    - `pty_active_count` (lines 162-167): doc comment + #[tauri::command] + fn body
    - `pty_close_all` (lines 169-177): doc comment + #[tauri::command] + fn body
    Keep: pty_create, pty_write, pty_resize, pty_close, pty_detach, pty_is_active, pty_get_session_info, pty_attach, pty_check_tmux, pty_list_tmux

    **filesystem.rs — Delete this 1 function (lines ~601-630):**
    - `detect_docs_available`: #[tauri::command] attribute + fn body
    Keep: all other filesystem commands

    **activity.rs — Delete these 3 functions AND their supporting types (lines ~160-313):**
    - `get_decisions` (lines 160-201): #[tauri::command] + fn body
    - `get_all_decisions` (lines 203-293): #[tauri::command] + fn body
    - `get_decision_categories` (lines 295-313): #[tauri::command] + fn body
    - Delete `DecisionWithProject` struct (lines 13-30)
    - Delete `DecisionFilters` struct (lines 33-41)
    - Remove `Decision` from the `use crate::models::{ActivityEntry, Decision};` import line (keep ActivityEntry)
    Keep: get_activity_log, search_activity

    **settings.rs — Delete this 1 function (lines ~160-202):**
    - `check_claude_status`: #[tauri::command] + fn body
    - Remove `ClaudeStatus` from the `use crate::models::{ClaudeStatus, Settings};` import line (keep Settings)
    Keep: get_settings, update_settings, reset_settings, import_settings

    **dependencies.rs — Delete these 2 commands + ALL their helper infrastructure:**
    - `run_dependency_audit` (lines 312-337): #[tauri::command] + fn body
    - `get_outdated_packages` (lines 731-750): #[tauri::command] + fn body
    - Delete `AuditResult` struct and impl (lines 268-310)
    - Delete `audit_npm` helper function (lines 339-477)
    - Delete `audit_cargo` helper function (lines 479-612)
    - Delete `audit_pip` helper function (lines 616-710)
    - Delete `OutdatedPackage` struct (lines ~720-729)
    - Delete `outdated_npm` helper function (lines 752-799)
    - Delete `outdated_cargo` helper function (lines 801-854)
    Keep: get_dependency_status, invalidate_dependency_cache, run_npm_audit, run_cargo_audit, run_pip_audit and all their supporting code (lines 1-310 approximately, minus AuditResult)

    **logs.rs — Delete this 1 function + its return type:**
    - `rotate_app_logs` (lines 198-233): doc comment + #[tauri::command] + fn body
    - Delete `LogRotationResult` struct (lines 236+)
    Keep: get_app_logs, get_app_log_stats, get_log_levels, clear_app_logs, log_frontend_error, log_frontend_event

    **models/mod.rs — Delete orphaned model structs:**
    - Delete `DocsAvailable` struct (line 188+) — only consumer was detect_docs_available
    - Delete `ClaudeStatus` struct (line 226+) — only consumer was check_claude_status
    - Keep `Decision` struct (line 68+) — still used by gsd.rs commands
    - After deleting, remove the corresponding `use` or `pub use` lines if they existed, and clean up any `#[derive(...)]` or `#[serde(...)]` that become orphaned

    **IMPORTANT:** After all deletions, also remove any `use` imports that become unused in each file (e.g., if `use std::process::Command;` was only needed by check_claude_status in settings.rs, remove it). Run `cargo check` after each file edit to catch unused import warnings.
  </action>
  <verify>
    <automated>cd /Users/jeremymcspadden/Github/track-your-shit && cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | grep -E "^error" | head -20; echo "---"; grep -c "pty_list_sessions\|pty_active_count\|pty_close_all\|detect_docs_available\|get_decisions\|get_all_decisions\|get_decision_categories\|check_claude_status\|run_dependency_audit\|get_outdated_packages\|rotate_app_logs" src-tauri/src/lib.rs; echo "Expected: 0"</automated>
  </verify>
  <acceptance_criteria>
    - lib.rs generate_handler![] contains zero references to: pty_list_sessions, pty_active_count, pty_close_all, detect_docs_available, get_decisions, get_all_decisions, get_decision_categories, check_claude_status, run_dependency_audit, get_outdated_packages, rotate_app_logs
    - `grep -c "pty_list_sessions" src-tauri/src/lib.rs` returns 0
    - `grep -c "fn pty_list_sessions" src-tauri/src/commands/pty.rs` returns 0
    - `grep -c "fn pty_active_count" src-tauri/src/commands/pty.rs` returns 0
    - `grep -c "fn pty_close_all" src-tauri/src/commands/pty.rs` returns 0
    - `grep -c "fn detect_docs_available" src-tauri/src/commands/filesystem.rs` returns 0
    - `grep -c "fn get_decisions" src-tauri/src/commands/activity.rs` returns 0
    - `grep -c "fn get_all_decisions" src-tauri/src/commands/activity.rs` returns 0
    - `grep -c "fn get_decision_categories" src-tauri/src/commands/activity.rs` returns 0
    - `grep -c "fn check_claude_status" src-tauri/src/commands/settings.rs` returns 0
    - `grep -c "fn run_dependency_audit" src-tauri/src/commands/dependencies.rs` returns 0
    - `grep -c "fn get_outdated_packages" src-tauri/src/commands/dependencies.rs` returns 0
    - `grep -c "fn rotate_app_logs" src-tauri/src/commands/logs.rs` returns 0
    - `grep -c "AuditResult" src-tauri/src/commands/dependencies.rs` returns 0
    - `grep -c "OutdatedPackage" src-tauri/src/commands/dependencies.rs` returns 0
    - `grep -c "LogRotationResult" src-tauri/src/commands/logs.rs` returns 0
    - `grep -c "DocsAvailable" src-tauri/src/models/mod.rs` returns 0
    - `grep -c "ClaudeStatus" src-tauri/src/models/mod.rs` returns 0
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0 (ignore pre-existing icon warning)
  </acceptance_criteria>
  <done>All 11 orphaned Rust commands removed from generate_handler![] and their function bodies, helper functions, and associated types deleted from source files. cargo check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify clean build, tests pass, and no remaining orphaned commands</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>
    src-tauri/src/lib.rs
  </read_first>
  <action>
    Run the full verification suite to confirm the gap is closed:

    1. Run `pnpm build` — must exit 0 with no TypeScript errors
    2. Run `pnpm test` — must report 130 passed / 0 failed (or more passed, 0 failed)
    3. Run a final orphan audit: extract every command name from generate_handler![] in lib.rs, then grep each one against src/ to confirm every registered command has at least one frontend invoke() caller. Report any remaining orphans. The pattern to search for each command is the command name string in an invoke() call (e.g., `invoke("pty_create"` or `invoke<...>("pty_create"`).

    If the audit finds any additional orphans not in the original 11, log them but do NOT remove them — they are out of scope for this gap closure. Document them in the SUMMARY.

    If `pnpm test` or `pnpm build` fails, diagnose and fix. The changes in Task 1 are Rust-only and should not affect TypeScript tests or build, but verify.
  </action>
  <verify>
    <automated>cd /Users/jeremymcspadden/Github/track-your-shit && pnpm build 2>&1 | tail -5 && echo "---" && pnpm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits with code 0
    - `pnpm test` output contains "0 failed" or shows all tests passed with no failures
    - Every command in lib.rs generate_handler![] has at least one matching invoke() call in src/ (verified by grep)
  </acceptance_criteria>
  <done>pnpm build clean, pnpm test 130+ passed / 0 failed, orphan audit confirms zero remaining orphaned commands in lib.rs.</done>
</task>

</tasks>

<verification>
1. `grep -c "pty_list_sessions\|pty_active_count\|pty_close_all\|detect_docs_available\|get_decisions\|get_all_decisions\|get_decision_categories\|check_claude_status\|run_dependency_audit\|get_outdated_packages\|rotate_app_logs" src-tauri/src/lib.rs` returns 0
2. `cargo check --manifest-path src-tauri/Cargo.toml` exits 0
3. `pnpm build` exits 0
4. `pnpm test` reports 0 failures
5. Orphan audit: every command in generate_handler![] has a frontend caller
</verification>

<success_criteria>
- VERIFICATION.md success criterion #3 now passes: "No unused Rust commands remain registered in lib.rs without an active frontend caller"
- All 4 success criteria verified (was 3/4, now 4/4)
- Build and test quality bar maintained (0 errors, 0 failures)
</success_criteria>

<output>
After completion, create `.planning/phases/10-dead-code-removal-and-quality/10-03-SUMMARY.md`
</output>
