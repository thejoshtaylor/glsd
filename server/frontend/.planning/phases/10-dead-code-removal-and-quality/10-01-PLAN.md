---
phase: 10-dead-code-removal-and-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/src/commands/gsd2.rs
  - src/lib/tauri.ts
  - src/lib/query-keys.ts
  - src/lib/queries.ts
  - src/lib/__tests__/tauri-gsd2.test.ts
autonomous: true
requirements:
  - DEAD-01
  - DEAD-04

must_haves:
  truths:
    - "gsd2_detect_version and gsd2_get_roadmap_progress are not registered in lib.rs"
    - "No Rust function body exists for gsd2_detect_version or gsd2_get_roadmap_progress in gsd2.rs"
    - "No frontend invoke wrapper exists for gsd2GetRoadmapProgress or gsd2DetectVersion in tauri.ts"
    - "No orphaned query key references remain for removed commands"
    - "cargo build completes with no warnings related to dead code"
    - "Every command registered in lib.rs has at least one frontend caller in src/lib/tauri.ts"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "Command registration list without dead commands"
      contains: "gsd2::gsd2_list_milestones"
    - path: "src-tauri/src/commands/gsd2.rs"
      provides: "GSD-2 command handlers without dead functions"
    - path: "src/lib/tauri.ts"
      provides: "Invoke wrappers without dead command wrappers"
    - path: "src/lib/query-keys.ts"
      provides: "Query key factory without orphaned keys"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/commands/gsd2.rs"
      via: "generate_handler macro references"
      pattern: "commands::gsd2::"
    - from: "src/lib/tauri.ts"
      to: "src/lib/queries.ts"
      via: "import and usage of invoke wrappers"
      pattern: "import.*from.*tauri"
---

<objective>
Remove dead Rust commands and their full frontend stacks. Audit all registered commands for orphaned entries.

Purpose: Eliminate dead code paths (gsd2_detect_version, gsd2_get_roadmap_progress) across the full stack — Rust function, lib.rs registration, TypeScript invoke wrapper, query key, query hook, and test. Also audit all lib.rs registrations against frontend callers to catch any additional orphans.

Output: Clean command registry with no dead entries; clean frontend API layer with no unused wrappers.
</objective>

<execution_context>
@/Users/jeremymcspadden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremymcspadden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dead-code-removal-and-quality/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead Rust commands and lib.rs registrations</name>
  <files>src-tauri/src/lib.rs, src-tauri/src/commands/gsd2.rs</files>
  <read_first>
    src-tauri/src/lib.rs
    src-tauri/src/commands/gsd2.rs
  </read_first>
  <action>
1. In `src-tauri/src/commands/gsd2.rs`, delete the entire `gsd2_detect_version` function (starts at the `#[tauri::command]` annotation around line 1084 through the closing brace of the function). Also delete the entire `gsd2_get_roadmap_progress` function (starts at `#[tauri::command]` around line 1053 through its closing brace).

2. In `src-tauri/src/lib.rs`, remove these two lines from the `generate_handler![]` macro invocation:
   - `commands::gsd2::gsd2_detect_version,` (line ~286)
   - `commands::gsd2::gsd2_get_roadmap_progress,` (line ~291)

3. Run `cargo build` from `src-tauri/` and fix any compilation errors. Check for unused import warnings — if removing these functions leaves any `use` statements unused in gsd2.rs, remove those too.

4. Full audit: Extract every command name from the `generate_handler![]` list in lib.rs. For each command, verify it has a corresponding invoke wrapper in `src/lib/tauri.ts` (the snake_case Rust name maps to a camelCase TS wrapper calling `invoke("rust_command_name")`). List any additional orphaned commands found beyond the two known ones. If any are found, remove them using the same full-stack pattern.
  </action>
  <verify>
    <automated>cd src-tauri && cargo check 2>&1 | grep -E "warning|error" | head -20; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gsd2_detect_version" src-tauri/src/lib.rs` returns 0
    - `grep -c "gsd2_get_roadmap_progress" src-tauri/src/lib.rs` returns 0
    - `grep -c "gsd2_detect_version" src-tauri/src/commands/gsd2.rs` returns 0
    - `grep -c "gsd2_get_roadmap_progress" src-tauri/src/commands/gsd2.rs` returns 0
    - `cd src-tauri && cargo check 2>&1 | grep -c "warning\[unused"` returns 0
  </acceptance_criteria>
  <done>Both dead Rust commands removed from gsd2.rs and unregistered from lib.rs. Cargo compiles with no unused warnings. Full audit confirms no other orphaned commands exist.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead frontend wrappers, query keys, hooks, and tests</name>
  <files>src/lib/tauri.ts, src/lib/query-keys.ts, src/lib/queries.ts, src/lib/__tests__/tauri-gsd2.test.ts</files>
  <read_first>
    src/lib/tauri.ts
    src/lib/query-keys.ts
    src/lib/queries.ts
    src/lib/__tests__/tauri-gsd2.test.ts
  </read_first>
  <action>
1. In `src/lib/tauri.ts`:
   - Delete the `Gsd2RoadmapProgressData` interface (lines ~1404-1411)
   - Delete the `gsd2GetRoadmapProgress` export function (lines ~1425-1426)
   - There is no `gsd2DetectVersion` wrapper in tauri.ts (confirmed by grep), so nothing to remove for that one

2. In `src/lib/query-keys.ts`:
   - Check if there is a query key for gsd2 roadmap progress. There is no `gsd2RoadmapProgress` key in the gsd2 section (the `gsdRoadmapProgress` key at line 94 is for the GSD v1 command `gsd_get_roadmap_progress`, NOT gsd2 — leave it). No changes needed here unless the audit in Task 1 found additional orphans.

3. In `src/lib/queries.ts`:
   - Search for any hook that uses `gsd2GetRoadmapProgress` or `Gsd2RoadmapProgressData`. If found, remove the hook. If no hook exists (wrapper was unused at the query layer), no changes needed.

4. In `src/lib/__tests__/tauri-gsd2.test.ts`:
   - Remove the import of `gsd2GetRoadmapProgress` from line 16
   - Delete the test case `'gsd2GetRoadmapProgress calls invoke with projectId'` (lines 44-47)

5. Run `pnpm build` to verify no TypeScript errors from the removals.
  </action>
  <verify>
    <automated>cd /Users/jeremymcspadden/Github/track-your-shit && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gsd2GetRoadmapProgress" src/lib/tauri.ts` returns 0
    - `grep -c "Gsd2RoadmapProgressData" src/lib/tauri.ts` returns 0
    - `grep -c "gsd2GetRoadmapProgress" src/lib/__tests__/tauri-gsd2.test.ts` returns 0
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>All frontend artifacts for dead commands removed. TypeScript build passes with zero errors. Test file updated to remove tests for deleted wrappers.</done>
</task>

</tasks>

<verification>
- `cargo check` in src-tauri/ produces zero warnings about unused code
- `pnpm build` completes with zero TypeScript errors
- `grep -r "gsd2_detect_version\|gsd2_get_roadmap_progress\|gsd2DetectVersion\|gsd2GetRoadmapProgress\|Gsd2RoadmapProgressData" src/ src-tauri/src/` returns only comments or zero results
</verification>

<success_criteria>
- Zero dead Rust commands registered in lib.rs without a frontend caller
- Zero orphaned TypeScript invoke wrappers for removed Rust commands
- Both `cargo check` and `pnpm build` pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-dead-code-removal-and-quality/10-01-SUMMARY.md`
</output>
