---
phase: 09-visual-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/globals.css
  - tailwind.config.js
  - src/lib/design-tokens.ts
  - src/hooks/use-theme.ts
  - src/components/theme/theme-provider.tsx
  - src/components/settings/theme-customization.tsx
  - src-tauri/tauri.conf.json
  - src/pages/todos.tsx
  - src/pages/projects.tsx
  - src/pages/dashboard.tsx
  - src/pages/logs.tsx
  - src/components/ui/progress.tsx
  - src/components/ui/input.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/terminal/terminal-tabs.tsx
  - src/components/terminal/global-terminals.tsx
  - src/components/projects/project-card.tsx
  - src/components/projects/bulk-project-bar.tsx
  - src/components/project/tab-group.tsx
  - src/components/project/roadmap-progress-card.tsx
  - src/components/project/requirements-card.tsx
  - src/components/project/project-overview-tab.tsx
  - src/components/project/project-terminal-tab.tsx
  - src/components/project/gsd-milestones-tab.tsx
  - src/components/project/gsd-plans-tab.tsx
  - src/components/project/gsd-context-tab.tsx
  - src/components/project/dependency-alerts-card.tsx
  - src/components/project/file-browser.tsx
  - src/components/project/dependencies-tab.tsx
  - src/components/project/activity-feed.tsx
  - src/components/notifications/notification-item.tsx
  - src/components/layout/main-layout.tsx
  - src/components/knowledge/knowledge-viewer.tsx
  - src/components/dashboard/project-card.tsx
  - src/components/dashboard/project-row.tsx
  - src/components/dashboard/status-bar.tsx
autonomous: true
requirements:
  - VISL-01
  - VISL-03
  - VISL-04

must_haves:
  truths:
    - "App renders with pure black background (hsl(0 0% 0%)), not the old blue-gray"
    - "All interactive elements (buttons, focus rings, links) use cyan accent, not blue"
    - "No brand-blue, brand-purple, or brand-yellow Tailwind classes remain anywhere in the source"
    - "Theme accent presets (ocean/forest/sunset/purple) are removed from CSS and settings UI"
    - "Window launches with black background (no white flash before React loads)"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "New .dark block with black/white/cyan palette; no :root light-mode color block; no theme-ocean/forest/sunset/purple CSS"
      contains: "--gsd-cyan"
    - path: "tailwind.config.js"
      provides: "gsd.cyan color map replacing brand.*; glow shadows referencing --gsd-cyan"
      contains: "gsd-cyan"
    - path: "src/lib/design-tokens.ts"
      provides: "projectTypeConfig and systemGroupConfig using gsd-cyan classes"
      contains: "gsd-cyan"
    - path: "src-tauri/tauri.conf.json"
      provides: "backgroundColor #000000 in window config"
      contains: "backgroundColor"
  key_links:
    - from: "tailwind.config.js"
      to: "src/styles/globals.css"
      via: "CSS variable references"
      pattern: "var\\(--gsd-cyan\\)"
    - from: "src/components/**/*.tsx"
      to: "tailwind.config.js"
      via: "Tailwind class names"
      pattern: "gsd-cyan"
---

<objective>
Migrate the entire CSS design token system from the old blue-gray-purple palette to the gsd.build black/white/cyan identity. Remove the light theme, delete dead brand tokens, rename brand-cyan to gsd-cyan, update all 33 files using old brand class names, simplify the theme provider (remove accent presets), and set the window background to black.

Purpose: The app must visually match gsd.build's dark-first identity -- pure black backgrounds, white text, cyan as the sole accent color.
Output: Updated globals.css, tailwind.config.js, design-tokens.ts, theme system files, tauri.conf.json, and all 33 component files with brand class references migrated.
</objective>

<execution_context>
@/Users/jeremymcspadden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremymcspadden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-visual-identity/09-CONTEXT.md
@.planning/phases/09-visual-identity/09-RESEARCH.md
@.planning/phases/09-visual-identity/09-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/hooks/use-theme.ts:
```typescript
export type Theme = "light" | "dark" | "system";
export type AccentColor = "default" | "ocean" | "forest" | "sunset" | "purple";
export type UiDensity = "compact" | "normal" | "spacious";
export type FontScale = "sm" | "md" | "lg";
export type FontFamily = "system" | "inter" | "jetbrains-mono" | "monospace";
```

From src/lib/design-tokens.ts:
```typescript
export type ProjectType = "gsd" | "bare";
export const projectTypeConfig: Record<ProjectType, { label: string; classes: string; tooltip: string }>;
export type SystemGroup = "gsd";
export const systemGroupConfig: Record<SystemGroup, { label: string; color: string; bgTint: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate CSS tokens, Tailwind config, and theme system</name>
  <files>
    src/styles/globals.css,
    tailwind.config.js,
    src/hooks/use-theme.ts,
    src/components/theme/theme-provider.tsx,
    src/components/settings/theme-customization.tsx,
    src-tauri/tauri.conf.json
  </files>
  <read_first>
    src/styles/globals.css,
    tailwind.config.js,
    src/hooks/use-theme.ts,
    src/components/theme/theme-provider.tsx,
    src/components/settings/theme-customization.tsx,
    src-tauri/tauri.conf.json,
    .planning/phases/09-visual-identity/09-RESEARCH.md
  </read_first>
  <action>
**globals.css changes:**

1. DELETE the entire `:root { }` color block (lines 9-48) — this is the light mode palette. DO NOT delete the `:root { --spacing-scale: 0.8; }` block at line 261.

2. REPLACE the `.dark { }` block (lines 50-85) with the new palette:
```css
.dark {
  --background: 0 0% 0%;
  --foreground: 0 0% 98%;
  --card: 0 0% 5%;
  --card-foreground: 0 0% 98%;
  --popover: 0 0% 5%;
  --popover-foreground: 0 0% 98%;
  --primary: 189 94% 43%;
  --primary-foreground: 0 0% 100%;
  --secondary: 0 0% 8%;
  --secondary-foreground: 0 0% 98%;
  --muted: 0 0% 8%;
  --muted-foreground: 0 0% 65%;
  --accent: 0 0% 12%;
  --accent-foreground: 0 0% 98%;
  --destructive: 0 62.8% 30.6%;
  --destructive-foreground: 0 0% 98%;
  --border: 0 0% 15%;
  --input: 0 0% 8%;
  --ring: 189 94% 43%;
  --radius: 0.5rem;

  /* GSD VibeFlow Brand Color */
  --gsd-cyan: 189 94% 50%;

  /* Status Colors - Semantic (Dark - higher contrast) */
  --status-success: 142 76% 52%;
  --status-warning: 38 95% 58%;
  --status-error: 0 88% 68%;
  --status-info: 217 91% 68%;
  --status-pending: 220 12% 58%;
  --status-blocked: 30 95% 58%;
  --status-paused: 45 100% 58%;
}
```

3. UPDATE `.text-gradient` (around line 171) — replace `--brand-blue` / `--brand-purple` gradient:
```css
.text-gradient {
  background: linear-gradient(to right, hsl(var(--gsd-cyan)), hsl(var(--foreground)));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
```

4. DELETE all theme accent preset blocks — `.theme-ocean`, `.theme-forest`, `.theme-sunset`, `.theme-purple` AND their `.dark.*` overrides (lines 186-231).

5. UPDATE `.dark .nav-item-active` (around line 234) — replace `--brand-blue` with `--gsd-cyan`:
```css
.dark .nav-item-active {
  box-shadow: 0 0 12px -2px hsl(var(--gsd-cyan) / 0.35),
    inset 0 0 0 1px hsl(var(--gsd-cyan) / 0.1);
}
```

**tailwind.config.js changes:**

1. Update file header from "Track Your Shit" to "GSD VibeFlow" (line 1).

2. REPLACE the `brand` color map with `gsd`:
```javascript
gsd: {
  cyan: "hsl(var(--gsd-cyan))",
},
```

3. REPLACE `boxShadow` — delete `glow-purple`, update others to `--gsd-cyan`:
```javascript
boxShadow: {
  'glow-sm': '0 0 10px hsl(var(--gsd-cyan) / 0.2)',
  'glow': '0 0 20px hsl(var(--gsd-cyan) / 0.3)',
  'glow-lg': '0 0 30px hsl(var(--gsd-cyan) / 0.4)',
},
```

**use-theme.ts changes:**

1. Simplify `AccentColor` type to only `"default"`:
```typescript
export type AccentColor = "default";
```

2. Simplify `Theme` type to only `"dark" | "system"` — remove `"light"`:
```typescript
export type Theme = "dark" | "system";
```

**theme-provider.tsx changes:**

1. Simplify `ACCENT_CLASSES` to only contain `default`:
```typescript
const ACCENT_CLASSES: Record<AccentColor, string> = {
  default: "",
};
```

2. Simplify `getInitialAccent()` to always return `"default"`:
```typescript
function getInitialAccent(): AccentColor {
  return "default";
}
```

3. Simplify `getInitialTheme()` — remove `"light"` branch, default to `"dark"`:
```typescript
function getInitialTheme(): Theme {
  if (typeof window === "undefined") return "dark";
  const stored = localStorage.getItem(THEME_KEY);
  if (stored === "dark" || stored === "system") {
    return stored;
  }
  return "dark";
}
```

4. In `setTheme` callback and the backend sync `useEffect`, keep the same logic but the type constraint now prevents `"light"`.

5. Keep all the `applyAccent`, `applyDensity`, `applyFontScale`, `applyFontFamily` functions — they still work correctly with the simplified types.

**theme-customization.tsx changes:**

1. DELETE the entire "Accent Color" section (the `ACCENT_OPTIONS` array definition at lines 12-48 and the JSX rendering block at lines 80-123 that renders the accent color picker buttons).

2. Remove the `accentColor, setAccentColor` destructuring from the `useTheme()` call.

3. Remove the `AccentColor` import from the import statement.

4. Keep the Density, Font Size, and Font Family sections unchanged.

**tauri.conf.json changes:**

1. Add `"backgroundColor": "#000000"` to the `app.windows[0]` object (after `"center": true`).
  </action>
  <verify>
    <automated>cd /Users/jeremymcspadden/Github/track-your-shit && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--gsd-cyan: 189 94% 50%` in the `.dark` block
    - globals.css contains `--background: 0 0% 0%` in the `.dark` block
    - globals.css does NOT contain `--brand-blue` or `--brand-purple` or `--brand-yellow`
    - globals.css does NOT contain `.theme-ocean` or `.theme-forest` or `.theme-sunset` or `.theme-purple`
    - globals.css contains `--spacing-scale: 0.8` (density block preserved)
    - globals.css `.nav-item-active` references `--gsd-cyan` not `--brand-blue`
    - globals.css `.text-gradient` references `--gsd-cyan` not `--brand-blue`
    - tailwind.config.js contains `gsd:` color key with `cyan:` subkey referencing `--gsd-cyan`
    - tailwind.config.js does NOT contain `brand:` color key
    - tailwind.config.js does NOT contain `glow-purple` box shadow
    - tailwind.config.js `glow-sm`, `glow`, `glow-lg` reference `--gsd-cyan`
    - use-theme.ts `AccentColor` type is `"default"` only (no ocean/forest/sunset/purple)
    - theme-provider.tsx `ACCENT_CLASSES` has only `default` key
    - theme-customization.tsx does NOT contain `ACCENT_OPTIONS` or accent color picker JSX
    - tauri.conf.json contains `"backgroundColor": "#000000"` in windows config
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>CSS tokens migrated to black/white/cyan palette. Light mode removed. Theme accent presets removed. Tailwind brand colors renamed to gsd. Window background set to black. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep all 33 component files — replace brand class names with gsd-cyan</name>
  <files>
    src/lib/design-tokens.ts,
    src/pages/todos.tsx,
    src/pages/projects.tsx,
    src/pages/dashboard.tsx,
    src/pages/logs.tsx,
    src/components/ui/progress.tsx,
    src/components/ui/input.tsx,
    src/components/ui/badge.tsx,
    src/components/ui/button.tsx,
    src/components/ui/card.tsx,
    src/components/terminal/terminal-tabs.tsx,
    src/components/terminal/global-terminals.tsx,
    src/components/projects/project-card.tsx,
    src/components/projects/bulk-project-bar.tsx,
    src/components/project/tab-group.tsx,
    src/components/project/roadmap-progress-card.tsx,
    src/components/project/requirements-card.tsx,
    src/components/project/project-overview-tab.tsx,
    src/components/project/project-terminal-tab.tsx,
    src/components/project/gsd-milestones-tab.tsx,
    src/components/project/gsd-plans-tab.tsx,
    src/components/project/gsd-context-tab.tsx,
    src/components/project/dependency-alerts-card.tsx,
    src/components/project/file-browser.tsx,
    src/components/project/dependencies-tab.tsx,
    src/components/project/activity-feed.tsx,
    src/components/notifications/notification-item.tsx,
    src/components/layout/main-layout.tsx,
    src/components/knowledge/knowledge-viewer.tsx,
    src/components/dashboard/project-card.tsx,
    src/components/dashboard/project-row.tsx,
    src/components/dashboard/status-bar.tsx
  </files>
  <read_first>
    src/lib/design-tokens.ts,
    src/pages/todos.tsx,
    src/components/layout/main-layout.tsx,
    src/components/project/roadmap-progress-card.tsx,
    src/components/projects/project-card.tsx,
    src/components/dashboard/status-bar.tsx,
    .planning/phases/09-visual-identity/09-RESEARCH.md
  </read_first>
  <action>
Perform a find-and-replace sweep across all 33 files (excluding globals.css and tailwind.config.js which were handled in Task 1). The rules are:

**Class name migration rules:**
- `brand-cyan` → `gsd-cyan` (1-for-1 rename everywhere — bg-brand-cyan becomes bg-gsd-cyan, text-brand-cyan becomes text-gsd-cyan, border-brand-cyan becomes border-gsd-cyan, etc.)
- `brand-blue` → `gsd-cyan` (blue is replaced by cyan in the new palette — all bg-brand-blue, text-brand-blue, border-brand-blue become bg-gsd-cyan, text-gsd-cyan, border-gsd-cyan)
- `brand-purple` → `gsd-cyan` (purple is replaced by cyan — same pattern)
- `brand-yellow` → context-dependent replacement:
  - If the element is a warning badge, caution indicator, or warning-adjacent visual cue → replace with `status-warning` (e.g., `text-brand-yellow` → `text-status-warning`, `bg-brand-yellow/10` → `bg-status-warning/10`)
  - If the element is a pure brand accent (decorative highlight, feature badge, etc.) → replace with `gsd-cyan`
  - READ each brand-yellow usage in its surrounding JSX context before deciding

**For `glow-purple` shadow class references:** Replace with `glow` (which now references cyan via tailwind config).

**Specific known changes in design-tokens.ts:**

`projectTypeConfig.gsd.classes`:
```typescript
classes: "bg-gsd-cyan/10 text-gsd-cyan border-gsd-cyan/20",
```

`systemGroupConfig.gsd`:
```typescript
gsd: {
  label: "GSD",
  color: "text-gsd-cyan",
  bgTint: "bg-gsd-cyan/20",
},
```

**Execution approach:**
1. First grep all files for `brand-blue`, `brand-purple`, `brand-yellow`, `brand-cyan` to get exact line numbers
2. Open each file, read the context around each occurrence
3. Apply the replacement rule
4. After all files are done, run the verification grep to confirm zero remaining old brand references
  </action>
  <verify>
    <automated>cd /Users/jeremymcspadden/Github/track-your-shit && grep -rn "brand-blue\|brand-purple\|brand-yellow\|brand-cyan" src/ --include="*.tsx" --include="*.ts" --include="*.css" --include="*.js" | grep -v node_modules | grep -v ".planning/" ; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "brand-blue\|brand-purple\|brand-yellow\|brand-cyan" src/ --include="*.tsx" --include="*.ts" --include="*.css"` returns zero results
    - design-tokens.ts `projectTypeConfig.gsd.classes` contains `gsd-cyan` (not `brand-purple`)
    - design-tokens.ts `systemGroupConfig.gsd.color` is `text-gsd-cyan` (not `text-brand-purple`)
    - design-tokens.ts `systemGroupConfig.gsd.bgTint` is `bg-gsd-cyan/20` (not `bg-brand-purple/20`)
    - `pnpm build` exits with code 0
    - `pnpm test --run` completes (pre-existing 4 failures acceptable, no NEW failures)
  </acceptance_criteria>
  <done>All 33 component files migrated from old brand class names to gsd-cyan. Zero occurrences of brand-blue, brand-purple, brand-yellow, or brand-cyan remain in source. Build and tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -rn "brand-blue\|brand-purple\|brand-yellow\|brand-cyan" src/ --include="*.tsx" --include="*.ts" --include="*.css" --include="*.js"` returns zero results
2. `grep -rn "theme-ocean\|theme-forest\|theme-sunset\|theme-purple" src/ --include="*.tsx" --include="*.ts" --include="*.css"` returns zero results
3. `pnpm build` exits 0
4. `pnpm test --run` reports no NEW test failures beyond the 4 pre-existing ones
5. globals.css contains `--background: 0 0% 0%` and `--gsd-cyan: 189 94% 50%`
6. tauri.conf.json contains `"backgroundColor": "#000000"`
</verification>

<success_criteria>
- App palette is pure black/white/cyan — no blue-gray tones remain in token definitions
- Zero old brand token references in any source file
- Theme system simplified to dark-only with no accent presets
- Window background is black (no white flash on launch)
- Build passes clean, no new test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-identity/09-01-SUMMARY.md`
</output>
