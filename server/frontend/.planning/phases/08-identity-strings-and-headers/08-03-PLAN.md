---
phase: 08-identity-strings-and-headers
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - "src-tauri/src/**/*.rs"
  - "src-tauri/build.rs"
  - "src/**/*.ts"
  - "src/**/*.tsx"
  - "src/**/*.css"
autonomous: true
requirements:
  - HDRS-01
  - HDRS-02

must_haves:
  truths:
    - "Every .rs file header reads 'GSD VibeFlow - [purpose]'"
    - "Every .ts/.tsx file header reads 'GSD VibeFlow - [purpose]'"
    - "A codebase-wide search for 'Track Your Shit' returns zero results"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "Rust library root with updated header"
      contains: "// GSD VibeFlow -"
    - path: "src/main.tsx"
      provides: "Frontend entry with updated header"
      contains: "// GSD VibeFlow -"
    - path: "src/App.tsx"
      provides: "App component with updated header"
      contains: "// GSD VibeFlow -"
  key_links:
    - from: "all source files"
      to: "file headers"
      via: "line 1 comment pattern"
      pattern: "// GSD VibeFlow -"
---

<objective>
Bulk-update every source file header from "Track Your Shit" to "GSD VibeFlow" across all .rs, .ts, .tsx, and .css files, then verify zero legacy name references remain in the entire codebase.

Purpose: Completes the rebrand by updating the last remaining references -- file headers in all 176+ source files. After this plan, a codebase-wide search for "Track Your Shit" returns zero results.
Output: All source file headers updated. Final verification confirms complete rename.
</objective>

<execution_context>
@/Users/jeremymcspadden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremymcspadden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-identity-strings-and-headers/08-CONTEXT.md
@.planning/phases/08-identity-strings-and-headers/08-01-SUMMARY.md
@.planning/phases/08-identity-strings-and-headers/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk-update all .rs file headers</name>
  <files>
    src-tauri/build.rs,
    src-tauri/src/lib.rs,
    src-tauri/src/main.rs,
    src-tauri/src/headless.rs,
    src-tauri/src/security.rs,
    src-tauri/src/db/mod.rs,
    src-tauri/src/db/tracing_layer.rs,
    src-tauri/src/models/mod.rs,
    src-tauri/src/pty/mod.rs,
    src-tauri/src/commands/mod.rs,
    src-tauri/src/commands/activity.rs,
    src-tauri/src/commands/data.rs,
    src-tauri/src/commands/dependencies.rs,
    src-tauri/src/commands/filesystem.rs,
    src-tauri/src/commands/git.rs,
    src-tauri/src/commands/gsd.rs,
    src-tauri/src/commands/gsd2.rs,
    src-tauri/src/commands/knowledge.rs,
    src-tauri/src/commands/logs.rs,
    src-tauri/src/commands/notifications.rs,
    src-tauri/src/commands/projects.rs,
    src-tauri/src/commands/pty.rs,
    src-tauri/src/commands/search.rs,
    src-tauri/src/commands/secrets.rs,
    src-tauri/src/commands/settings.rs,
    src-tauri/src/commands/snippets.rs,
    src-tauri/src/commands/terminal.rs,
    src-tauri/src/commands/watcher.rs
  </files>
  <read_first>
    - src-tauri/src/lib.rs (line 1 to confirm current header format)
    - src-tauri/build.rs (line 1 to confirm current header format)
  </read_first>
  <action>
For every .rs file listed above (27 files + build.rs = 28 files total), replace line 1:

`// Track Your Shit - [purpose text]` -> `// GSD VibeFlow - [purpose text]`

The purpose text after the dash stays exactly the same. Only "Track Your Shit" changes to "GSD VibeFlow". The copyright line (line 2 or 3) stays unchanged.

Use sed or a script to perform the replacement efficiently:
```bash
find src-tauri -name "*.rs" -exec sed -i '' 's|^// Track Your Shit -|// GSD VibeFlow -|' {} +
sed -i '' 's|^// Track Your Shit -|// GSD VibeFlow -|' src-tauri/build.rs
```

Also update the Cargo.toml header comment if not already done:
```bash
sed -i '' 's|^# Track Your Shit -|# GSD VibeFlow -|' src-tauri/Cargo.toml
```

After the bulk replace, verify with:
```bash
grep -rn "Track Your Shit" src-tauri/ --include="*.rs" --include="*.toml"
```
This should return zero results.
  </action>
  <verify>
    <automated>grep -rc "Track Your Shit" src-tauri/ --include="*.rs" | grep -v ":0$" | wc -l | xargs test 0 -eq && echo "PASS: All .rs headers updated" || echo "FAIL: Legacy headers remain"</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/lib.rs line 1 contains `// GSD VibeFlow - Library Root`
    - src-tauri/src/main.rs line 1 contains `// GSD VibeFlow - Main Entry Point`
    - src-tauri/build.rs line 1 contains `// GSD VibeFlow - Build Script`
    - grep -r "Track Your Shit" src-tauri/ --include="*.rs" returns zero results
    - grep -r "Track Your Shit" src-tauri/Cargo.toml returns zero results
    - All 28 .rs files have `// GSD VibeFlow -` on line 1
  </acceptance_criteria>
  <done>All 28 Rust source files have updated headers. Zero "Track Your Shit" references in any .rs file.</done>
</task>

<task type="auto">
  <name>Task 2: Bulk-update all .ts/.tsx/.css file headers and run final verification</name>
  <files>
    All 149 .ts/.tsx files under src/,
    src/styles/globals.css
  </files>
  <read_first>
    - src/main.tsx (line 1 to confirm header format)
    - src/App.tsx (line 1 to confirm header format)
    - src/styles/globals.css (line 1 to confirm CSS header format)
  </read_first>
  <action>
For every .ts and .tsx file under src/ (149 files), replace line 1:

`// Track Your Shit - [purpose text]` -> `// GSD VibeFlow - [purpose text]`

For src/styles/globals.css, replace line 1:
`/* Track Your Shit - Global Styles` -> `/* GSD VibeFlow - Global Styles`

Use sed for efficient bulk replacement:
```bash
find src -name "*.ts" -o -name "*.tsx" | xargs sed -i '' 's|^// Track Your Shit -|// GSD VibeFlow -|'
sed -i '' 's|^/\* Track Your Shit -|/* GSD VibeFlow -|' src/styles/globals.css
```

After all headers are updated, run a FINAL CODEBASE-WIDE verification:
```bash
grep -rn "Track Your Shit" src/ src-tauri/ README.md CLAUDE.md package.json index.html website/index.html *.md
```

This MUST return zero results. If any remain, fix them.

Then run the build to confirm nothing is broken:
```bash
pnpm build
```

And run tests to confirm updated test assertions pass:
```bash
pnpm test
```
  </action>
  <verify>
    <automated>grep -r "Track Your Shit" src/ src-tauri/ README.md CLAUDE.md package.json index.html website/index.html 2>/dev/null | wc -l | xargs test 0 -eq && echo "PASS: Zero legacy references in entire codebase" || echo "FAIL: Legacy references remain"</automated>
  </verify>
  <acceptance_criteria>
    - src/main.tsx line 1 contains `// GSD VibeFlow - Main Entry Point`
    - src/App.tsx line 1 contains `// GSD VibeFlow - Main App Component`
    - src/styles/globals.css line 1 contains `/* GSD VibeFlow - Global Styles`
    - grep -r "Track Your Shit" src/ returns zero results
    - grep -r "Track Your Shit" src-tauri/ returns zero results
    - grep -r "Track Your Shit" README.md CLAUDE.md package.json index.html website/index.html returns zero results
    - pnpm build exits with code 0
    - pnpm test exits with code 0 (or only pre-existing failures unrelated to rename)
  </acceptance_criteria>
  <done>All 149 TypeScript/TSX files and 1 CSS file have updated headers. Codebase-wide grep for "Track Your Shit" returns zero results. Build and tests pass.</done>
</task>

</tasks>

<verification>
- `grep -r "Track Your Shit" src/ src-tauri/ README.md CLAUDE.md package.json index.html website/index.html` returns ZERO results
- `grep -r "track-your-shit" src-tauri/tauri.conf.json package.json src-tauri/Cargo.toml` returns ZERO results (except track-your-shit.db which is an actual filename)
- `pnpm build` exits 0
- `pnpm test` passes
- `find src -name "*.ts" -o -name "*.tsx" | xargs head -1 | grep "Track Your Shit" | wc -l` returns 0
- `find src-tauri -name "*.rs" | xargs head -1 | grep "Track Your Shit" | wc -l` returns 0
</verification>

<success_criteria>
A codebase-wide search for "Track Your Shit" returns zero results across ALL committed files. Every source file header reads "GSD VibeFlow - [purpose]". The build compiles and tests pass. The rebrand is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/08-identity-strings-and-headers/08-03-SUMMARY.md`
</output>
