---
phase: 08-identity-strings-and-headers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/components/layout/main-layout.tsx
  - src/components/settings/theme-customization.tsx
  - src/components/settings/export-data-dialog.tsx
  - src/components/projects/import-project-dialog.tsx
  - src/pages/settings.tsx
  - src/pages/project.tsx
  - src/pages/projects.test.tsx
  - src/components/layout/main-layout.test.tsx
  - src/styles/globals.css
  - src-tauri/src/commands/pty.rs
  - src-tauri/src/pty/mod.rs
  - src-tauri/src/commands/git.rs
  - README.md
  - CLAUDE.md
  - website/index.html
autonomous: true
requirements:
  - IDNT-02
  - STRN-01
  - STRN-02
  - STRN-03
  - STRN-04

must_haves:
  truths:
    - "Sidebar header displays 'GSD VibeFlow' instead of 'Track Your Shit'"
    - "Settings page description says 'GSD VibeFlow'"
    - "All user-facing dialogs and messages reference 'GSD VibeFlow'"
    - "README.md and CLAUDE.md describe the app as 'GSD VibeFlow'"
    - "Test assertions reference 'GSD VibeFlow'"
    - "index.html page title says 'GSD VibeFlow'"
    - "website/index.html contains zero 'Track Your Shit' references"
  artifacts:
    - path: "src/components/layout/main-layout.tsx"
      provides: "Sidebar with updated app name"
      contains: "GSD VibeFlow"
    - path: "index.html"
      provides: "Root HTML with updated page title"
      contains: "GSD VibeFlow"
    - path: "README.md"
      provides: "Project documentation with new name"
      contains: "GSD VibeFlow"
    - path: "CLAUDE.md"
      provides: "Claude instructions with new name"
      contains: "GSD VibeFlow"
    - path: "website/index.html"
      provides: "Marketing website with new name"
      contains: "GSD VibeFlow"
  key_links:
    - from: "src/components/layout/main-layout.tsx"
      to: "sidebar UI"
      via: "hardcoded string on line 105"
      pattern: "GSD VibeFlow"
    - from: "src/pages/projects.test.tsx"
      to: "test assertions"
      via: "expected text content"
      pattern: "GSD VibeFlow"
    - from: "index.html"
      to: "browser tab title"
      via: "<title> tag on line 7"
      pattern: "GSD VibeFlow"
---

<objective>
Replace all user-facing "Track Your Shit" strings in UI components, pages, test files, Rust doc comments, documentation, root index.html, and the marketing website with "GSD VibeFlow".

Purpose: Ensures every user-visible text in the running app, every developer-facing document, the HTML page title, and the marketing site references the new brand name.
Output: Zero "Track Your Shit" strings in any UI component, test assertion, Rust doc comment, documentation file, or HTML file.
</objective>

<execution_context>
@/Users/jeremymcspadden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremymcspadden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-identity-strings-and-headers/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all UI strings, page title, and Rust doc comments referencing old name</name>
  <files>
    index.html,
    src/components/layout/main-layout.tsx,
    src/components/settings/theme-customization.tsx,
    src/components/settings/export-data-dialog.tsx,
    src/components/projects/import-project-dialog.tsx,
    src/pages/settings.tsx,
    src/pages/project.tsx,
    src/pages/projects.test.tsx,
    src/components/layout/main-layout.test.tsx,
    src/styles/globals.css,
    src-tauri/src/commands/pty.rs,
    src-tauri/src/pty/mod.rs,
    src-tauri/src/commands/git.rs
  </files>
  <read_first>
    - index.html (line 7: page title)
    - src/components/layout/main-layout.tsx (line 105: sidebar app name)
    - src/components/settings/theme-customization.tsx (line 77: CardDescription string)
    - src/components/settings/export-data-dialog.tsx (line 84: export description string)
    - src/components/projects/import-project-dialog.tsx (line 186: importing status string)
    - src/pages/settings.tsx (line 71: page description, line 127: auto-launch text)
    - src/pages/project.tsx (line 365: remove project confirmation text)
    - src/pages/projects.test.tsx (line 49: mock project name, line 153: assertion)
    - src/styles/globals.css (line 31 and 71: CSS comment "Brand Colors - Track Your Shit Identity")
    - src-tauri/src/commands/pty.rs (line 82: doc comment)
    - src-tauri/src/pty/mod.rs (line 34 and 209: doc comments)
    - src-tauri/src/commands/git.rs (line 345: stash message string)
  </read_first>
  <action>
Replace every "Track Your Shit" occurrence in the following files (excluding line-1 file headers which are handled in plan 08-03):

**Root index.html (page title -- STRN-02):**
- `index.html:7` -- `<title>Track Your Shit</title>` -> `<title>GSD VibeFlow</title>`

**TypeScript/TSX UI strings:**
- `src/components/layout/main-layout.tsx:105` -- `Track Your Shit` -> `GSD VibeFlow` (sidebar header text)
- `src/components/settings/theme-customization.tsx:77` -- `Customize the look and feel of Track Your Shit` -> `Customize the look and feel of GSD VibeFlow`
- `src/components/settings/export-data-dialog.tsx:84` -- `Export Track Your Shit data to a file for backup or migration.` -> `Export GSD VibeFlow data to a file for backup or migration.`
- `src/components/projects/import-project-dialog.tsx:186` -- `Importing project into Track Your Shit...` -> `Importing project into GSD VibeFlow...`
- `src/pages/settings.tsx:71` -- `description="Configure Track Your Shit preferences"` -> `description="Configure GSD VibeFlow preferences"`
- `src/pages/settings.tsx:127` -- `Launch Track Your Shit when you log in` -> `Launch GSD VibeFlow when you log in`
- `src/pages/project.tsx:365` -- `from Track Your Shit.` -> `from GSD VibeFlow.`

**Test files (full treatment per CONTEXT.md decision):**
- `src/pages/projects.test.tsx:49` -- `name: "Track Your Shit"` -> `name: "GSD VibeFlow"` (mock project data)
- `src/pages/projects.test.tsx:153` -- `expect(screen.getByText("Track Your Shit"))` -> `expect(screen.getByText("GSD VibeFlow"))`

**CSS comments:**
- `src/styles/globals.css:31` -- `/* Brand Colors - Track Your Shit Identity */` -> `/* Brand Colors - GSD VibeFlow Identity */`
- `src/styles/globals.css:71` -- `/* Brand Colors - Track Your Shit Identity (Dark) */` -> `/* Brand Colors - GSD VibeFlow Identity (Dark) */`

**Rust doc comments and strings (non-header):**
- `src-tauri/src/commands/pty.rs:82` -- `/// List all Track Your Shit tmux sessions` -> `/// List all GSD VibeFlow tmux sessions`
- `src-tauri/src/pty/mod.rs:34` -- `/// Info about a tmux session managed by Track Your Shit` -> `/// Info about a tmux session managed by GSD VibeFlow`
- `src-tauri/src/pty/mod.rs:209` -- `/// List all Track Your Shit tmux sessions` -> `/// List all GSD VibeFlow tmux sessions`
- `src-tauri/src/commands/git.rs:345` -- `"Track Your Shit stash"` -> `"GSD VibeFlow stash"` (git stash message)

**Note on toast/notification coverage (STRN-03):** Toast messages in this app are produced inline by components and hooks using the sonner toast library. The grep-based verify step below will catch any remaining "Track Your Shit" references across all .tsx/.ts files, including any toast calls. No separate toast-specific files need to be enumerated -- the broad grep covers them.

After making all changes, run `grep -rn "Track Your Shit" src/ src-tauri/src/ index.html --include="*.tsx" --include="*.ts" --include="*.css" --include="*.rs" --include="*.html"` and verify only line-1 file headers remain.
  </action>
  <verify>
    <automated>grep -rn "Track Your Shit" src/ src-tauri/src/ index.html --include="*.tsx" --include="*.ts" --include="*.css" --include="*.rs" --include="*.html" | grep -v "^.*:1:" | wc -l | xargs test 0 -eq && echo "PASS: Only headers remain" || echo "FAIL: Non-header references still present"</automated>
  </verify>
  <acceptance_criteria>
    - index.html line 7 contains `<title>GSD VibeFlow</title>`
    - src/components/layout/main-layout.tsx contains the string `GSD VibeFlow` near line 105
    - src/pages/settings.tsx contains `Configure GSD VibeFlow preferences`
    - src/pages/settings.tsx contains `Launch GSD VibeFlow when you log in`
    - src/pages/projects.test.tsx contains `name: "GSD VibeFlow"`
    - src/pages/projects.test.tsx contains `getByText("GSD VibeFlow")`
    - src/styles/globals.css contains `Brand Colors - GSD VibeFlow Identity`
    - src-tauri/src/commands/git.rs contains `"GSD VibeFlow stash"`
    - grep -rn "Track Your Shit" src/ src-tauri/src/ index.html | grep -v ":1:" returns zero lines (only file headers remain)
  </acceptance_criteria>
  <done>All user-facing UI strings, test assertions, CSS comments, Rust doc comments/strings, and the root HTML page title reference "GSD VibeFlow". Only line-1 file headers still contain the old name (deferred to plan 08-03).</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md, CLAUDE.md, and website/index.html documentation</name>
  <files>README.md, CLAUDE.md, website/index.html</files>
  <read_first>
    - README.md (full file -- need to update title, description, and any body references)
    - CLAUDE.md (full file -- update project overview and file header convention)
    - website/index.html (full file -- 10+ occurrences of "Track Your Shit" in title, meta, nav, hero, body, footer)
  </read_first>
  <action>
**README.md:**
- Replace the `# Track Your Shit` title with `# GSD VibeFlow`
- Replace the `<!-- Track Your Shit - Project README -->` comment with `<!-- GSD VibeFlow - Project README -->`
- Replace any other occurrences of "Track Your Shit" in the body text with "GSD VibeFlow"
- Keep all technical content, instructions, and structure intact

**CLAUDE.md:**
- In "## Project Overview" section: replace `Track Your Shit is a native desktop application` with `GSD VibeFlow is a native desktop application`
- In "## File Header Convention" section: replace the example `// Track Your Shit - [File Purpose]` with `// GSD VibeFlow - [File Purpose]`
- Keep all technical instructions, architecture docs, and command references intact
- Do NOT change paths like `track-your-shit.db` in the Database section (that is the actual SQLite filename on disk, not a display name -- changing it would break existing installations)

Search entire file for any remaining "Track Your Shit" and replace with "GSD VibeFlow".

**website/index.html:**
Replace ALL occurrences of "Track Your Shit" with "GSD VibeFlow" throughout the file. Key locations:
- Line 2: `<!-- Track Your Shit - Marketing Website -->` -> `<!-- GSD VibeFlow - Marketing Website -->`
- Line 8: `<title>Track Your Shit - Desktop App for GSD Workflows</title>` -> `<title>GSD VibeFlow - Desktop App for GSD Workflows</title>`
- Line 18: OG title meta tag
- Line 183: Nav brand text `Track Your Shit` -> `GSD VibeFlow`
- Lines 223-225: Hero heading "Track Your" / "Shit." -> "GSD" / "VibeFlow." (adjust layout as needed)
- Line 404: Body text `Track Your Shit natively understands` -> `GSD VibeFlow natively understands`
- Line 265: Dashboard alt text
- Line 451: Project detail alt text
- Line 722: "Here's everything else packed into Track Your Shit." -> "Here's everything else packed into GSD VibeFlow."
- Line 846: "Ready to track your shit?" -> "Ready to get shit done?" (or similar rebrand-appropriate CTA)
- Line 912: Footer copyright `Track Your Shit` -> `GSD VibeFlow`

Do a thorough search for any remaining "Track Your Shit" in website/index.html after changes. Also update alt text on images that reference the old name.

Note: Do NOT change GitHub URLs or release asset filenames (those are actual URLs that must remain functional until new releases are cut).
  </action>
  <verify>
    <automated>grep -c "Track Your Shit" README.md CLAUDE.md website/index.html index.html | grep -v ":0$" | wc -l | xargs test 0 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README.md first line contains `# GSD VibeFlow`
    - README.md contains no occurrences of "Track Your Shit"
    - CLAUDE.md contains `GSD VibeFlow is a native desktop application`
    - CLAUDE.md file header convention shows `// GSD VibeFlow - [File Purpose]`
    - CLAUDE.md contains no occurrences of "Track Your Shit"
    - CLAUDE.md still contains `track-your-shit.db` (database filename unchanged)
    - website/index.html contains no occurrences of "Track Your Shit"
    - website/index.html title tag contains "GSD VibeFlow"
    - website/index.html nav brand text is "GSD VibeFlow"
    - website/index.html footer copyright says "GSD VibeFlow"
    - index.html contains no occurrences of "Track Your Shit"
  </acceptance_criteria>
  <done>README.md, CLAUDE.md, and website/index.html fully describe the app as "GSD VibeFlow" with zero legacy name references. Technical content and functional URLs preserved.</done>
</task>

</tasks>

<verification>
- `grep -rn "Track Your Shit" src/ src-tauri/src/ README.md CLAUDE.md index.html website/index.html --include="*.tsx" --include="*.ts" --include="*.css" --include="*.rs" --include="*.md" --include="*.html" | grep -v "^.*:1:"` returns only file headers in source files
- `grep -c "Track Your Shit" README.md CLAUDE.md index.html website/index.html` returns 0 for all files
- `pnpm build` compiles successfully (no broken string references)
- `pnpm test` passes (updated test assertions match new strings)
</verification>

<success_criteria>
Every user-facing string, test assertion, CSS comment, Rust doc comment, documentation file, root HTML page title, and marketing website page references "GSD VibeFlow". A grep for "Track Your Shit" across all source, doc, and HTML files returns only line-1 file headers (to be handled by plan 08-03).
</success_criteria>

<output>
After completion, create `.planning/phases/08-identity-strings-and-headers/08-02-SUMMARY.md`
</output>
